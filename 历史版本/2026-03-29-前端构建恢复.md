# 2026-03-29 前端构建恢复

## 调整目标

- 恢复 `web/` 前端项目的正常构建能力。

## 本次修改

- 重新确认并写回 `web/package.json` 为 UTF-8 无 BOM。
- 修复 `SavingsGoalDialog.tsx` 与 `SavingsPlanDialog.tsx` 中的语法损坏。

## 结果

- Turbopack 不再因 `package.json` 解析或储蓄弹窗语法错误而中断。
- `web/` 构建已恢复。

## 验证记录

- `web/` 目录执行 `npm run build` 通过。
