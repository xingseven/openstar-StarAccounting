# 2026-04-03 修复消费页懒加载图表组件残留语法错误

## 本次变更

- 清理 `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 中 `LazyChart` 组件后的重复 `return` 残留代码。
- 移除未完成的 `motion.div` 包装残片，恢复懒加载图表组件的正常函数结构。

## 涉及文件

- `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx`
- `CHANGELOG.md`
- `docs/开发进度.md`

## 验证

- `npm run build`

## 结果说明

- 消费页主题组件已恢复正常语法结构。
- `Return statement is not allowed here` 构建错误已消失。
