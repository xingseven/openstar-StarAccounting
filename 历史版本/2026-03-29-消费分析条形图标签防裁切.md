# 2026-03-29 消费分析条形图标签防裁切

## 调整目标

- 避免消费页分析条形图右侧的数值标签超出卡片容器。

## 本次修改

- 增加预算偏差、大额预警、时段热点图表的右侧留白。
- 调整条形图标签距离。
- 放宽周末偏好图的 X 轴最大值，给 `100%` 标签留出显示空间。

## 结果

- 条形图右侧标签可以完整显示。
- 预算与风险、场景偏好这两组图表的可读性更稳定。

## 验证记录

- `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/shared/mockData.ts` 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。
