# 2026-03-29 复购与集中度图标签裁切修正

## 调整目标

- 修正“复购与集中”模块中集中度图右侧百分比标签贴边和裁切的问题。

## 本次修改

- 增加集中度图右侧安全留白。
- 放宽集中度图 X 轴最大值到 `110`。
- 保持右侧标签展示方式不变。

## 结果

- 集中度图右侧百分比标签可以完整显示。
- 复购与集中模块的两张图在视觉上更平衡。

## 验证记录

- `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/shared/mockData.ts` 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。
