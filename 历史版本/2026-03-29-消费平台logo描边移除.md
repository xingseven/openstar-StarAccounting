# 2026-03-29 消费平台 Logo 描边移除

## 调整目标

- 去掉消费页平台图标外层的圆形描边，让平台列表更干净。

## 本次修改

- 移除支付宝图标容器的 `ring` 描边。
- 移除微信图标容器的 `ring` 描边。
- 同步移除默认平台图标容器的 `ring` 描边。

## 结果

- Logo 区域更干净。
- 平台图标的存在感保留，但不会再被外层描边强调。

## 验证记录

- `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
- `npm run typecheck` 通过。
