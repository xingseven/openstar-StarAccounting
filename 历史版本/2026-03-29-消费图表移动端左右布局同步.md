# 2026-03-29 消费图表移动端左右布局同步

## 调整目标

- 让消费页“支付渠道占比”和“本期收入与支出”两张图表卡片在移动端也保持与 PC 一致的左右布局。

## 本次修改

- 两张卡片的内容区改为移动端默认双列布局。
- 移动端图表尺寸调整为 `84px`。
- `sm` 以上仍保持 `104px` 图表尺寸。

## 结果

- 移动端阅读方式和 PC 更一致。
- 图表信息不再先图后文，而是同步横向对照。
- 右侧文字与数值仍保持足够可读性。

## 验证记录

- `npm run typecheck` 通过。
- `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
- `npm run build` 通过。
