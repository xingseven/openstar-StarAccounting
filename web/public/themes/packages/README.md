# Theme Packages

Put your theme preview images here.
File names should match `previewImage` field in `registry.ts`.
E.g., `DefaultDashboard.png` will be accessible at `/themes/packages/DefaultDashboard.png`.
