version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: xfdashboard-db
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: xfdashboard
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ./src/server
    container_name: xfdashboard-server
    environment:
      DATABASE_URL: "mysql://root:password@mysql:3306/xfdashboard"
      PORT: 3001
      PUBLIC_IP: "0.0.0.0"
      JWT_SECRET: "dev-secret"
      ALLOW_DEV_HEADERS: 1
    ports:
      - "3001:3001"
    depends_on:
      mysql:
        condition: service_healthy
    restart: always

  web:
    build:
      context: ./web
    container_name: xfdashboard-web
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost:3001"
    ports:
      - "3000:3000"
    depends_on:
      - server
    restart: always

volumes:
  postgres_data:

