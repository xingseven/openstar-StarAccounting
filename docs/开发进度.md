# 开发进度

## 当前目标

- M1 基础底座：完成前后端工程初始化与基础路由/接口骨架

## 已完成

- 前端：Next.js App Router（web/），包含 Dashboard 路由组与基础布局（Sidebar/Header）
- 前端：Query Provider 已接入（TanStack Query）
- 后端：Express（src/server），包含 `/api/health` 与 `/api/connect/*` 骨架

## 下一步

- 数据层：引入 Prisma + PostgreSQL，落地核心 Schema（User/Transaction/Loan/AppConnection 等）
- 鉴权：实现 Auth 登录与 JWT（Web 与 APP Token 区分）
- 连接管理：Web 端生成 OTP 与设备列表页面联动后端

