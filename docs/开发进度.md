﻿# 开发进度

## 当前目标

- V2.3.100：消费页恢复分析图表并补齐骨架一致性 ✅
- V2.3.99：共享筛选器与底部弹层跟随主题收口 ✅
- V2.3.98：消费页默认主题对齐 DefaultDashboard 工作台骨架 ✅
- V2.3.97：默认主题页族公共骨架迁移收口 ✅
- V2.3.96：补充主题接入公共骨架框架的文档接线说明 ✅
- V2.3.95：资产页默认主题对齐 DefaultDashboard 蓝绿卡片工作台 ✅
- V2.3.94：抽取公共真实骨架框架并统一核心业务页 LoadingShell ✅
- V2.3.93：修复默认主题总览页骨架屏与真实模块结构不一致 ✅
- V2.3.92：统一移动端筛选面板年月切换为步进器样式 ✅
- V2.3.91：隔离总览页筛选浮层开关状态，修复图表重复重绘 ✅
- V2.3.90：修复默认主题总览页侧边栏切换被 Dashboard 路由同步抢占 ✅
- V2.3.89：同步主题文档迁移边界并清理残留空 src 目录 ✅
- V2.3.88：后端统计链路收口并优化总览聚合性能 ✅
- V2.3.85：修复总览页全部时间筛选误回退到本月 ✅
- V2.3.84：总览页筛选接入真实数据查询与缓存 ✅
- V2.3.83：公共悬浮筛选器补齐非受控交互，修复总览页按钮失效 ✅
- V2.3.82：后端目录收口到根目录 server ✅
- V2.3.81：删除旧前端目录并完成有效差异迁移 ✅
- V2.3.80：前端目录收口并补齐旧前端清理清单 ✅
- V2.3.79：总览页正式改为 Dashboard 文件名路由 ✅
- V2.3.78：总览页文件名提示改为路由内联展示 ✅
- V2.3.77：总览页可见显示当前 Dashboard 文件名 ✅
- V2.3.76：web 前端并回主仓库统一追踪 ✅
- V2.3.75：总览路由补充当前 Dashboard 文件定位信息 ✅
- V2.3.74：主题系统改为 token + manifest + registry 分层架构 ✅
- V2.3.67：概览卡右侧趋势图去掉圆点与月份文字 ✅
- V2.3.66：概览卡图标移到左上角，趋势线收口到右半边 ✅
- V2.3.65：资金走势改成纯白卡片与浅色叠层面积折线效果 ✅
- V2.3.64：资金走势折线图改为柔和多层波峰面积效果 ✅
- V2.3.63：去掉概览卡趋势图中的柱状块，仅保留折线趋势 ✅
- V2.3.62：概览卡加高并改为最近3个月折线瀑布趋势 ✅
- V2.3.61：总览概览卡补充瀑布感微型图表 ✅
- V2.3.60：总览页改为蓝绿色轻量工作台并收起顶部冗长描述 ✅
- V2.3.59：修复消费页资金流向与交易明细区损坏 JSX 文本 ✅
- V2.3.58：修复消费页商户集中与分析卡片区损坏 JSX 文本 ✅
- V2.3.57：修复消费页图表分析区损坏模板字符串与 JSX 文案 ✅
- V2.3.56：修复消费页筛选浮层损坏 JSX 文本 ✅
- V2.3.55：修复消费页自定义时间筛选区未闭合字符串 ✅
- V2.3.54：修复消费页懒加载图表组件残留语法错误 ✅
- V2.3.53：修复消费页主题组件非法 import 构建错误 ✅
- V2.3.52：修复 3000 端口页面源码编码损坏与运行时异常 ✅
- V2.3.51：修复 useInsertionEffect 运行时异常 ✅
- V2.3.50：默认主题改为更现代的冷白清爽配色 ✅
- V2.3.49：默认主题重做为暖白简约风并统一首页模块视觉 ✅
- V2.3.48：根目录 npm 脚本恢复指向旧版 web 项目 ✅
- V2.3.47：清理已废弃前端迁移记录与临时远端分支 ✅
- V2.3.29：页面切换预热与主页面切换性能优化 ✅
- V2.3.23：数据页工作区重排、AI 页面交互收口与动效优化 ✅
- V2.3.22：规则备注收口、明细列表统一与消费页细节修正 ✅
- V2.3.21：订单号强约束导入、规则备注分析与非 Docker 自动部署 ✅
- V2.3.18：消费分析面板增强、图表布局优化与标签防裁切修复 ✅
- V2.3.17：模块主题分色与去毛玻璃主题收敛 ✅
- V2.3.16：交易对方自动归类、AI 模型配置修复与 AI 页面概览调整 ✅
- V2.3.12：主题系统收尾、验收新主题并打通全量校验 ✅
- V2.3.11：统一总览页资金体检为指标卡样式 ✅
- V2.3.10：调整总览页资金体检模块布局与位置 ✅
- V2.3.7：全局主题系统与 dashboard 页面主题统一 ✅
- V2.3.3：修复账单导入重复数据统计显示为0的Bug ✅
- V2.3.2：账单导入优化（统一分类） ✅
- V2.3.1：数据管理与账户管理功能 ✅
- V2.3.0：多账户权限系统 ✅
- V2.2.6：遵循 Next.js App Router 最佳实践，重构加载状态并解决刷新滚动回顶的终极问题 ✅

## 已完成

### V2.3.100：消费页恢复分析图表并补齐骨架一致性 (2026-04-05)

- **开发范围评估**：
  - 本轮属于消费页默认主题对齐后的纠偏修复，不额外新建专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 更新 `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx`，保留 Dashboard 工作台卡片语言，同时把“资金流向”区块恢复为真实流向图，而不是摘要条形图。
  - 保持商户排行、热区、结构洞察、重复商户、预算偏差、备注洞察、堆叠趋势、热力日历、散点图和直方图等分析模块继续存在，避免消费页再次退化成只剩基础三行工作台。
  - 扩展 `web/src/features/consumption/components/ConsumptionLoadingShell.tsx`，让骨架屏继续跟随恢复后的分析区块密度，而不是停留在删减后的 4 + 3 + 3 结构。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充“主题对齐默认不能主动删减原页面关键图表密度，除非用户明确要求裁剪”的规则。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.20`。

- **验收结果**：
  - `npm.cmd --prefix web run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/consumption/components/ConsumptionLoadingShell.tsx` 已通过。
  - `git diff --check -- web/src/features/consumption/components/ConsumptionDefaultTheme.tsx web/src/features/consumption/components/ConsumptionLoadingShell.tsx` 已通过。
  - `npm.cmd --prefix web run typecheck` 仍被仓库现有的 `web/src/app/(dashboard)/connections/page.tsx` 损坏字符 / JSX 语法错误阻塞，本轮未修改该文件。

### V2.3.99：共享筛选器与底部弹层跟随主题收口 (2026-04-05)

- **开发范围评估**：
  - 本轮属于共享筛选器 / 底部弹层主题跟随改造，不额外新建专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 更新 `web/src/components/shared/theme-primitives.tsx`，补充共享浮层面板、区块、触发器以及对话框输入框 / 选择器的主题原语类名，并让 `ThemeSectionHeader` 正式渲染 `description`。
  - 更新 `web/src/components/ui/bottomsheet.tsx`、`web/src/components/ui/select.tsx`，将底部弹层、拖拽条、关闭按钮、标题、副标题、下拉触发器、下拉内容、选项与分隔线统一切到主题 token。
  - 更新 `web/src/components/shared/FloatingFilter.tsx`，让移动端筛选底部弹层和桌面端筛选浮层通过共享主题原语与模块 accent 自动跟随当前主题，不再保留默认灰白壳层。
  - 更新 `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx`，显式将消费页共享筛选器接到 `module="consumption"`，并清理页面内遗留的未使用告警。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充“共享筛选器 / BottomSheet / Select 只通过 token 和主题原语换皮，不在共享组件里堆 `themeId` 分支”的规则。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.19`。

- **验收结果**：
  - `npm.cmd --prefix web run lint -- src/components/shared/FloatingFilter.tsx src/components/shared/theme-primitives.tsx src/components/ui/bottomsheet.tsx src/components/ui/select.tsx src/features/consumption/components/ConsumptionDefaultTheme.tsx` 已通过。
  - `git diff --check -- web/src/components/shared/theme-primitives.tsx web/src/components/ui/bottomsheet.tsx web/src/components/ui/select.tsx web/src/components/shared/FloatingFilter.tsx web/src/features/consumption/components/ConsumptionDefaultTheme.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已通过。
  - `npm.cmd run typecheck` 仍被仓库现有的 `web/src/app/(dashboard)/connections/page.tsx` 缺失主题原语导入错误阻塞，本轮未修改该文件。

### V2.3.98：消费页默认主题对齐 DefaultDashboard 工作台骨架 (2026-04-05)

- **开发范围评估**：
  - 本轮属于默认主题下消费页的既有样式对齐改造，不额外新建专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 重写 `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx`，将消费页切换为与 `DefaultDashboard.tsx` 同构的三行工作台布局。
  - 第一行统一为总支出、总收入、记录质量、消费排行四张卡；第二行为收支趋势、区间对比、分类结构三张图表卡；第三行为支出趋势、平台构成和近期流水表。
  - 保留共享悬浮筛选器、本地平台/关键词过滤、`只看备注` 开关，以及 `AI记一笔` 的识图记账弹层流程。
  - 同步重写 `web/src/features/consumption/components/ConsumptionLoadingShell.tsx`，使骨架屏和真实页面的 4 + 3 + 3 工作台结构一致。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充“功能页被明确要求与目标主题 100% 一致时，必须直接复用目标主题的工作台结构和 LoadingShell 镜像”的约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.18`。

- **验收结果**：
  - `npm.cmd --prefix web run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/consumption/components/ConsumptionLoadingShell.tsx` 已通过。
  - `git diff --check -- web/src/features/consumption/components/ConsumptionDefaultTheme.tsx web/src/features/consumption/components/ConsumptionLoadingShell.tsx` 已通过。
  - `npm.cmd --prefix web run typecheck` 仍被仓库现有的 `web/src/features/loans/components/themes/DefaultLoans.tsx` 缺失导入错误阻塞，本轮未修改该文件。

### V2.3.97：默认主题页族公共骨架迁移收口 (2026-04-05)

- **开发范围评估**：
  - 本轮属于默认主题页族的公共骨架收口，不额外新建专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **文档调整**：
  - 更新 `docs/主题开发框架文档.md`，新增“公共骨架迁移知识点”和“公共骨架迁移注意点”两组规则，并明确默认主题页族当前的迁移范围。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.17`。

- **代码调整**：
  - 扩充 `web/src/components/shared/PageLoadingShell.tsx`，新增默认主题工作台语义的共享骨架块，包括工作台卡片壳、卡片头、强调指标卡、进度卡、趋势列表、图表卡和表格卡。
  - 重写 `web/src/features/dashboard/components/themes/DashboardLoadingShell.tsx`，改为基于共享工作台骨架块的薄封装，移除总览页局部重复维护的卡片与图表骨架壳。
  - 重写 `web/src/features/assets/components/themes/AssetsLoadingShell.tsx`，将资产页骨架收口到同一套共享工作台骨架块，只保留关注账户列表这一小段局部占位。
  - 现在默认主题页族中，消费页、储蓄页、借贷页、总览页、资产页都已经挂到公共骨架框架上；后续调整默认主题工作台布局时，优先修改共享块或页面薄封装层即可。

- **验收结果**：
  - `npm.cmd --prefix web run lint -- src/components/shared/PageLoadingShell.tsx src/features/dashboard/components/themes/DashboardLoadingShell.tsx src/features/assets/components/themes/AssetsLoadingShell.tsx` 已执行并通过。
  - `git diff --check -- web/src/components/shared/PageLoadingShell.tsx web/src/features/dashboard/components/themes/DashboardLoadingShell.tsx web/src/features/assets/components/themes/AssetsLoadingShell.tsx docs/主题开发框架文档.md` 已执行。

### V2.3.96：补充主题接入公共骨架框架的文档接线说明 (2026-04-05)

- **开发范围评估**：
  - 本轮属于主题框架文档补充说明，不额外新建专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **文档调整**：
  - 更新 `docs/主题开发框架文档.md`，补充“主题不会直接在 Manifest 声明骨架框架，而是通过 `dashboardVariant -> dashboard-registry -> XxxLoadingShell -> PageLoadingShell` 接入”的说明。
  - 补充“复用现有主题变体时如何自动复用骨架”“新建独立主题变体时如何基于 `PageLoadingShell` 组装 `XxxLoadingShell`”两条具体规则。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.16`。

- **验收结果**：
  - `git diff --check -- docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已执行。

### V2.3.95：资产页默认主题对齐 DefaultDashboard 蓝绿卡片工作台 (2026-04-05)

- **开发范围评估**：
  - 本轮属于默认主题下资产页的现有风格统一改造，不额外新建专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 重做 `web/src/features/assets/components/themes/DefaultAssets.tsx`，将资产页改成与 `DefaultDashboard.tsx` 一致的蓝/绿强调卡 + 白卡分析区布局。
  - 补齐资产净值、可动用资产、结构总览、重点持仓、账户类型、币种分布、管理面板、关注账户和资产账户列表等模块。
  - 同步更新 `web/src/features/assets/components/themes/AssetsLoadingShell.tsx`，让骨架屏跟随新的卡片层级和模块布局。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充“功能页明确要求跟随既有主题文件时，应优先复用该主题卡片语言和模块组织”的约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.15`。

- **验收结果**：
  - `npm.cmd --prefix web run lint -- src/features/assets/components/themes/DefaultAssets.tsx src/features/assets/components/themes/AssetsLoadingShell.tsx` 已执行并通过。
  - `git diff --check -- web/src/features/assets/components/themes/DefaultAssets.tsx web/src/features/assets/components/themes/AssetsLoadingShell.tsx` 已执行。
  - `npm.cmd --prefix web run typecheck` 仍被仓库现有的 `web/src/features/savings/components/themes/DefaultSavings.tsx:725` 语法错误阻塞，本轮未引入新的类型错误定位。

### V2.3.94：抽取公共真实骨架框架并统一核心业务页 LoadingShell (2026-04-05)

- **开发范围评估**：
  - 本轮属于前端加载态框架收敛，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 扩充 `web/src/components/shared/PageLoadingShell.tsx`，补充共享骨架块，包括紧凑 Hero、通用 Surface、表格骨架、列表骨架、图表骨架和卡片骨架导出。
  - 将 `DashboardLoadingShell.tsx`、`ConsumptionLoadingShell.tsx`、`AssetsLoadingShell.tsx`、`SavingsLoadingShell.tsx`、`LoansLoadingShell.tsx`、`DataLoadingShell.tsx` 收敛为“共享骨架框架 + 页面薄封装”的组合方式。
  - 现在核心业务页的 LoadingShell 不再各自重复维护整套 Hero、Metric、Chart、Table、List 占位结构，后续页面结构调整时只需优先修改共享块或薄封装层。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，新增共享加载骨架框架约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.14`。

- **验收结果**：
  - `git diff --check -- web/src/components/shared/PageLoadingShell.tsx web/src/features/dashboard/components/themes/DashboardLoadingShell.tsx web/src/features/consumption/components/ConsumptionLoadingShell.tsx web/src/features/assets/components/themes/AssetsLoadingShell.tsx web/src/features/savings/components/themes/SavingsLoadingShell.tsx web/src/features/loans/components/themes/LoansLoadingShell.tsx web/src/app/(dashboard)/data/DataLoadingShell.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已执行。
  - `npm.cmd --prefix web run typecheck` 在当前工作区被 `web/src/features/savings/components/themes/DefaultSavings.tsx` 的既有语法错误阻断，本轮未改动该文件的大段业务内容。

### V2.3.93：修复默认主题总览页骨架屏与真实模块结构不一致 (2026-04-05)

- **开发范围评估**：
  - 本轮属于默认主题总览页加载态一致性修补，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 更新 `web/src/features/dashboard/components/themes/DashboardLoadingShell.tsx`，移除已经失效的灰色大背景壳、外层圆角容器和 `min-h-screen`。
  - 将骨架屏外层容器的间距与 `DefaultDashboard.tsx` 当前真实页面保持一致。
  - 重做第三行骨架模块，改为和真实页面一致的“日度支出趋势 + 商户构成环图 + 最近交易表格”结构。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充 Dashboard 页面结构变化时必须同步更新 LoadingShell 的约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.13`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已执行。
  - `git diff --check -- web/src/features/dashboard/components/themes/DashboardLoadingShell.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已执行。

### V2.3.92：统一移动端筛选面板年月切换为步进器样式 (2026-04-05)

- **开发范围评估**：
  - 本轮属于共享筛选器交互样式统一的小型补丁，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 更新 `web/src/components/shared/FloatingFilter.tsx` 的 `CustomPeriodPicker`。
  - 移动端自定义时间不再使用“年份输入框 + 月份下拉选择”，改为直接复用桌面端的 `YearStepper` 和 `MonthStepper`。
  - 现在手机端与桌面端的年月切换都统一为左右按钮步进样式，减少额外展开选择层。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充移动端共享筛选器也应优先复用步进器的约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.12`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已执行。
  - `git diff --check -- web/src/components/shared/FloatingFilter.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已执行。

### V2.3.91：隔离总览页筛选浮层开关状态，修复图表重复重绘 (2026-04-05)

- **开发范围评估**：
  - 本轮属于总览页默认主题的渲染性能补丁修正，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 更新 `web/src/features/dashboard/components/themes/DefaultDashboard.tsx`，将右下角悬浮筛选器的 `isFilterOpen` 开关从 Dashboard 图表主体中隔离出去。
  - 将默认主题主体改为记忆化视图组件，仅在真实数据或筛选条件变化时重渲染；打开或关闭筛选浮层时不再整页重跑图表渲染。
  - 清理默认主题内已经失效的旧 Popover 遗留状态与未使用接线，收口当前唯一生效的共享悬浮筛选器入口。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充“纯 UI 浮层开关状态不能直接挂在重型图表组件树上”的约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.11`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已执行。
  - `git diff --check -- web/src/features/dashboard/components/themes/DefaultDashboard.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已执行。

### V2.3.90：修复默认主题总览页侧边栏切换被 Dashboard 路由同步抢占 (2026-04-05)

- **开发范围评估**：
  - 本轮属于总览页路由同步逻辑的补丁修正，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 更新 `web/src/features/dashboard/components/DashboardPageShell.tsx` 的 Dashboard 路由纠正条件。
  - 现在仅在“当前路径是某个 Dashboard 文件名别名，且该别名与当前主题不一致”时才执行 `router.replace`。
  - 根路径 `/` 保持作为稳定总览入口，不再在挂载后主动跳到 `/<Dashboard文件名>`，避免与默认主题侧边栏点击产生路由竞争，修复首击偶发不跳转和切换延迟偏高的问题。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充总览根入口 `/` 与 Dashboard 文件名别名路由的最新约定。
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.10`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `git diff --check -- web/src/features/dashboard/components/DashboardPageShell.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已通过。

### V2.3.89：同步主题文档迁移边界并清理残留空 src 目录 (2026-04-05)

- **开发范围评估**：
  - 本轮属于目录迁移后的文档收尾与残留目录清理，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次整理。

- **代码与目录调整**：
  - 更新 `docs/主题开发框架文档.md`，明确当前正式生效的前端目录固定为 `web/src`、后端目录固定为 `server/src`。
  - 补充说明根目录旧 `src/` 已不再承载正式源码，若本地仍存在 `src/server`，应视为迁移残留空目录。
  - 清理本地残留的空 `src/server` 目录，避免继续误导后续开发。

- **文档同步**：
  - `docs/主题开发框架文档.md` 小版本升级到 `v2.1.9`。
  - `CHANGELOG.md`、`docs/开发进度.md` 同步记录本次整理。

- **验收结果**：
  - `git diff --check -- docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已执行。
  - `Test-Path src` 已用于确认残留目录是否清理完成。

### V2.3.88：后端统计链路收口并优化总览聚合性能 (2026-04-05)

- **开发范围评估**：
  - 本轮属于后端稳定性与性能排查，不仅要确认服务是否真的掉线，还要处理“总览页一次拼多条接口”的慢链路问题。
  - 由于同时涉及后端接口设计、查询范围修正与总览页数据入口调整，本轮新增专项文档 `docs/后端稳定性与总览性能优化开发文档.md`。

- **代码调整**：
  - 在 `server/src/main.ts` 新增 `/api/dashboard/summary`，由后端一次性返回总览页所需的资产、负债、收支对比、最近交易与预算预警数据。
  - 修复 `/api/metrics/consumption/summary`，补上 `platform`、`search` 与 `accountId` 过滤，避免总览筛选条件传到后端后被静默忽略。
  - 将消费统计、资产、储蓄、预算等高频读取接口统一收口到当前账户，避免多账户数据混查。
  - 重写 `/api/budgets` 与 `/api/budgets/alerts` 的预算健康计算路径，去掉逐条预算单独聚合交易的 N+1 查询。
  - 更新 `web/src/features/dashboard/data-loader.ts`，总览页改为单请求聚合，不再由前端并发拼装 9 个接口结果。

- **验收结果**：
  - `npm.cmd --prefix server run build` 已通过。
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `npm.cmd run build` 已通过。
  - 在本地真实开发数据下，`/api/dashboard/summary` 热态响应约为 `86ms`。

### V2.3.85：修复总览页全部时间筛选误回退到本月 (2026-04-05)

- **开发范围评估**：
  - 本轮属于总览页筛选接线的补丁修正，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 修正 `web/src/features/dashboard/data-loader.ts` 的查询归一化逻辑。
  - 现在只有在完全不传 query 时才会使用“本月默认查询”，当用户在总览页选择“全部时间”时，`startDate/endDate` 会保持为空，不再被错误回填成默认本月。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充“全部时间”筛选的空区间处理约定。
  - 文档版本同步升级为 `v2.1.8`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `git diff --check -- web/src/features/dashboard/data-loader.ts docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已通过。

### V2.3.84：总览页筛选接入真实数据查询与缓存 (2026-04-05)

- **开发范围评估**：
  - 本轮属于总览页现有壳层与共享筛选器的接线修正，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 重写 `web/src/features/dashboard/components/DashboardPageShell.tsx`，补齐总览页的日期筛选、自定义时间、平台、关键词状态，并按筛选条件重新加载数据。
  - 重写 `web/src/features/dashboard/data-loader.ts`，新增 `DashboardQuery`，支持按日期区间、对比区间、平台和关键词查询，同时按查询维度拆分热缓存。
  - 更新 `web/src/features/dashboard/components/themes/DefaultDashboard.tsx` 与 `web/src/themes/dashboard-registry.tsx`，把平台、关键词和筛选回调透传到共享悬浮筛选器，保证 UI 与数据层联动。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充总览页共享筛选器的真实接线约定。
  - 文档版本同步升级为 `v2.1.7`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `git diff --check -- web/src/features/dashboard/data-loader.ts web/src/features/dashboard/components/DashboardPageShell.tsx web/src/themes/dashboard-registry.tsx web/src/features/dashboard/components/themes/DefaultDashboard.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已通过。

### V2.3.83：公共悬浮筛选器补齐非受控交互，修复总览页按钮失效 (2026-04-05)

- **开发范围评估**：
  - 本轮属于共享组件交互修正，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 在 `web/src/components/shared/FloatingFilter.tsx` 中为日期筛选、自定义时间、平台和关键词补齐受控 / 非受控双模式状态管理。
  - 当父页面未传入对应回调时，筛选浮层现在会自动回退到组件内部状态，不再出现“弹层能打开，但按钮点不动”的问题。
  - 默认主题总览页沿用现有接法时，浮层内的本月、全部时间、自定义、年份、月份等按钮均可正常交互。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充共享悬浮筛选器的复用约定。
  - 文档版本同步升级为 `v2.1.6`。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `git diff --check -- web/src/components/shared/FloatingFilter.tsx docs/主题开发框架文档.md docs/开发进度.md CHANGELOG.md` 已通过。

### V2.3.82：后端目录收口到根目录 server (2026-04-05)

- **开发范围评估**：
  - 本轮属于仓库结构收口，目标是将唯一后端工程从 `src/server/` 迁移到根目录 `server/`，避免仓库中继续保留二级 `src/server/src` 的混淆结构。
  - 由于影响范围覆盖根脚本、CI、Docker、非 Docker 部署链路与运维文档，本轮新增专项文档 `docs/后端目录收口开发文档.md` 记录迁移过程。

- **代码与目录调整**：
  - 将后端工程目录整体迁移为根目录 `server/`，保留现有 Prisma、脚本、源码、测试与已提交产物结构。
  - 更新根目录 `package.json`、`docker-compose.yml`、`.github/workflows/ci.yml`、`.github/workflows/deploy-non-docker.yml` 与部署脚本，统一改为引用 `server/`。
  - 同步更新 `README.md`、`docs/数据库配置.md`、`docs/数据库结构核对报告.md`、`docs/非Docker自动部署开发文档.md` 等当前仍会被使用的说明文档。
  - 将 `docs/前端目录收口开发文档.md` 小版本升级为 `v1.0.2`，把最终仓库结构改写为 `web/ + server/`。

- **验收结果**：
  - `npm.cmd --prefix server run build` 已通过。
  - `npm.cmd run build` 已通过。
  - `git diff --check -- package.json docker-compose.yml README.md CHANGELOG.md docs/开发进度.md docs/前端目录收口开发文档.md docs/后端目录收口开发文档.md .github/workflows/ci.yml .github/workflows/deploy-non-docker.yml server/scripts/deploy-linux.sh server/scripts/deploy-windows.ps1` 已通过。

### V2.3.81：删除旧前端目录并完成有效差异迁移 (2026-04-05)

- **开发范围评估**：
  - 本轮属于前端目录收口的第二阶段，目标是将确认仍有价值的旧前端差异迁入 `web/`，并真正删除旧前端源码与旧构建配置。
  - 继续沿用 `docs/前端目录收口开发文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次整理。

- **代码与目录调整**：
  - 在 `web/src/components/shared/AuthGate.tsx` 中补回 `setAuthUser`，支持登录/注册成功后直接注入用户缓存。
  - 在 `web/src/app/auth/login/page.tsx` 中恢复登录成功后的 `setAuthUser(data.user)`，并增加 `router.prefetch(next)`。
  - 在 `web/src/app/auth/register/page.tsx` 中恢复注册成功后的 `setAuthUser(data.user)`。
  - 将 `web/package.json` 的构建脚本切换为 `next build --webpack`，规避 Next.js 16 Turbopack 在 `/_global-error` 预渲染阶段的构建异常。
  - 将根目录 `package.json` 的 `build` 改为委托执行 `cd web && npm run build`，与前端正式构建链路保持一致。
  - 删除根目录旧前端源码目录：`src/app`、`src/components`、`src/features`、`src/lib`、`src/themes`、`src/types`。
  - 删除根目录旧前端配置：`components.json`、`next.config.ts`、`tsconfig.json`、`next-env.d.ts`、`public/`。
  - 更新 `README.md` 与 `docs/前端目录收口开发文档.md`，将状态改为“收口完成”。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `npm.cmd --prefix web run build -- --webpack` 已通过。
  - `npm.cmd run build` 已通过，现已统一走 `webpack` 构建链路。
  - `git diff --check -- README.md CHANGELOG.md docs/开发进度.md docs/前端目录收口开发文档.md web/src/app/auth/login/page.tsx web/src/app/auth/register/page.tsx web/src/components/shared/AuthGate.tsx Dockerfile package.json web/package.json` 已通过。

### V2.3.80：前端目录收口并补齐旧前端清理清单 (2026-04-05)

- **开发范围评估**：
  - 本轮属于仓库结构收口，涉及前端主入口、Docker 构建入口和后续删除旧前端的执行顺序。
  - 由于影响范围跨 `web/`、根目录配置和文档体系，本轮新增专项文档 `docs/前端目录收口开发文档.md` 记录整理策略。

- **代码与文档调整**：
  - 重写根目录 `README.md`，明确当前唯一生效的前端目录为 `web/`，后端目录为 `src/server/`。
  - 修正根目录 `Dockerfile`，使其从仓库根构建时直接打包 `web/` 前端，而不是继续沿用旧根目录前端产物路径。
  - 新增 `docs/前端目录收口开发文档.md`，整理 `src` 与 `web/src` 的重叠统计、27 个分叉文件、1 个旧前端独有文件和后续删除顺序。
  - 明确当前阶段先统一入口与文档，不直接删除旧前端源码，避免与现有未提交改动冲突。

- **验收结果**：
  - `npm.cmd --prefix web run typecheck` 已通过。
  - `git diff --check -- README.md Dockerfile CHANGELOG.md docs/开发进度.md docs/前端目录收口开发文档.md` 已通过。
  - 全仓 `git diff --check` 未通过，现有工作区中已有与本轮无关的 trailing whitespace，主要位于 `src/features/dashboard/components/themes/DefaultDashboard.tsx` 与 `报错.txt`。

### V2.3.79：总览页正式改为 Dashboard 文件名路由 (2026-04-05)

- **开发范围评估**：
  - 本轮属于总览路由结构调整，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 新增 `web/src/app/(dashboard)/[dashboardEntry]/page.tsx`，总览页可直接通过 `/<Dashboard文件名>` 访问。
  - `web/src/app/(dashboard)/page.tsx` 改为根入口页，根据当前主题自动跳转到对应的 Dashboard 文件名路由。
  - 新增总览路由工具与共享壳组件，并同步适配侧边栏、移动端导航、搜索与路由预热逻辑。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，说明总览页现已直接采用 `/<Dashboard文件名>` 路由。
  - 文档版本同步升级为 `v2.1.5`。

- **验收结果**：
  - `npm.cmd run typecheck` 已通过。

### V2.3.78：总览页文件名提示改为路由内联展示 (2026-04-05)

- **开发范围评估**：
  - 本轮属于小范围开发体验修正，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 将 `web/src/app/(dashboard)/page.tsx` 中的右下角悬浮提示改为页面顶部的内联提示条。
  - 保留当前 `themeId` 和 Dashboard 文件名显示，但不再遮挡总览页右下角筛选按钮。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，说明当前提示改为路由顶部内联展示。
  - 文档版本同步升级为 `v2.1.4`。

- **验收结果**：
  - `npm.cmd run typecheck` 已通过。

### V2.3.77：总览页可见显示当前 Dashboard 文件名 (2026-04-05)

- **开发范围评估**：
  - 本轮属于小范围开发体验增强，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 在 `web/src/app/(dashboard)/page.tsx` 中新增开发态右下角 `Dashboard Source` 浮层。
  - 浮层直接显示当前 `themeId` 和实际命中的 Dashboard 文件名，避免只看 DOM 属性不直观。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，说明总览文件名提示现已改为页面可见形式。
  - 文档版本同步升级为 `v2.1.3`。

- **验收结果**：
  - `npm.cmd run typecheck` 已通过。

### V2.3.76：web 前端并回主仓库统一追踪 (2026-04-05)

- **开发范围评估**：
  - 本轮属于仓库结构整理，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **仓库整理**：
  - 将 `web` 从根仓库中的 gitlink 目录调整为普通受管目录。
  - 后续 `web/src`、`web/public` 等前端文件可直接随主仓库提交和推送到 GitHub。

- **配套处理**：
  - 补齐根仓库对 `.next`、`playwright-report` 等前端构建产物的忽略规则。
  - 更新 `docs/主题开发框架文档.md`，说明 `web/` 已并回主仓库统一管理。

- **验收结果**：
  - `npm.cmd run typecheck` 已通过。

### V2.3.75：总览路由补充当前 Dashboard 文件定位信息 (2026-04-05)

- **开发范围评估**：
  - 本轮属于小范围的开发体验优化，不额外拆新的专项开发文档。
  - 继续沿用 `docs/主题开发框架文档.md`、`docs/开发进度.md` 和 `CHANGELOG.md` 记录本次调整。

- **代码调整**：
  - 在 `web/src/app/(dashboard)/page.tsx` 中接入 `getDashboardEntryFileName(themeId)`。
  - 给总览页根节点补充 `data-dashboard-entry-file`，方便直接定位当前主题实际命中的总览文件。

- **文档同步**：
  - 更新 `docs/主题开发框架文档.md`，补充从路由入口反查实际总览文件名的查找链路。
  - 文档版本同步升级为 `v2.1 / v2.1.1`。

- **验收结果**：
  - `npm.cmd run typecheck` 已通过。
  - `npm.cmd run build` 未通过，当前卡在 Next.js 预渲染 `/_global-error` 时的 `Expected workUnitAsyncStorage to have a store` 异常。

### V2.3.74：主题系统改为 token + manifest + registry 分层架构 (2026-04-05)

- **开发范围评估**：
  - 本轮属于中等规模的主题架构收敛，不额外拆新的专项开发文档。
  - 继续沿用 `docs/开发进度.md`、`CHANGELOG.md` 和 `docs/主题开发框架文档.md` 作为本轮记录链路。

- **架构调整**：
  - 新增 `web/src/themes/theme-manifest.ts`，将 Dashboard 变体、Header、Sidebar、移动端导航和主题预览风格统一收口到接线层。
  - 新增 `web/src/themes/dashboard-registry.tsx`，集中维护 Dashboard 动态加载注册表，移除总览页入口中的长串主题条件分支。
  - `web/src/components/shared/Header.tsx`、`Sidebar.tsx`、`MobileBottomNav.tsx` 和 `web/src/app/(dashboard)/themes/page.tsx` 改为读取 manifest，而不是继续直接判断主题名。

- **共享壳层收敛**：
  - 默认 Sidebar 主壳层改为更多依赖主题变量，便于 graphite、spruce、terracotta、nova、midnight、frost 这类主题复用默认侧栏骨架。
  - 主题选择页复制的 Dashboard 文件名改为读取真实注册表入口，避免显示未落地的占位文件名。

- **文档同步**：
  - 重写 `docs/主题开发框架文档.md`，将旧版“Dashboard 独立 + 其他逻辑换皮”的说明升级为当前的 `token -> manifest -> registry` 分层说明。
  - 明确新增主题时的接线步骤、拆文件边界和反模式清单。

- **验收结果**：
  - `npm.cmd run typecheck` 已通过。
  - `npm.cmd run build` 已通过。


### V2.3.67：概览卡右侧趋势图去掉圆点与月份文字 (2026-04-04)

- **图表收口**：
  - 去掉总览概览卡右侧趋势图中的圆点。
  - 去掉趋势图底部月份文字，只保留线条与面积层。

- **视觉结果**：
  - 让右半边图形更像纯装饰趋势线，减少信息干扰。
  - 整体更贴近参考图里“只有线和面”的简洁表达。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.66：概览卡图标移到左上角，趋势线收口到右半边 (2026-04-04)

- **卡片布局调整**：
  - 将总览概览卡的图标移到左上角，和参考图保持一致。
  - 文案信息集中在左侧区域，避免和图表上下堆叠。

- **图表位置调整**：
  - 将卡片里的趋势图从底部整行展示，改为右半边独立展示。
  - 同步收窄微图表宽度与高度，让右侧曲线更像参考图里的半边装饰图。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.65：资金走势改成纯白卡片与浅色叠层面积折线效果 (2026-04-04)

- **风格回调**：
  - 根据新的参考图，将“资金走势”从蓝绿氛围图回调为纯白卡片样式。
  - 顶部保留简洁标题与右上角轻量筛选胶囊，更贴近参考图结构。

- **图表重做**：
  - 使用浅蓝、深蓝、绿色三层柔和面积折线叠加，形成更接近示意图的波峰层次。
  - 图表背景、坐标轴、网格线与 tooltip 同步切回浅色体系，整体更轻更干净。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.64：资金走势折线图改为柔和多层波峰面积效果 (2026-04-04)

- **图表风格重做**：
  - 将总览页“资金走势”图表改为更接近参考图的柔和多层波峰面积效果。
  - 收入与支出曲线都改为更轻的发光描边与渐变面积叠加，弱化原先普通后台图表感。

- **视觉层次调整**：
  - 图表外层增加蓝绿雾感渐变背景与高光层，让走势图更有氛围感。
  - 坐标轴、网格线和 tooltip 一并改成更贴近参考图的浅色悬浮样式。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.63：去掉概览卡趋势图中的柱状块，仅保留折线趋势 (2026-04-04)

- **图表样式收口**：
  - 去掉总览概览卡趋势图里带有柱状感的瀑布块和连接块。
  - 卡片内部仅保留折线、节点、面积填充和月份标签，让走势表达更干净。

- **视觉调整**：
  - 维持当前蓝卡、绿卡、白卡的主题色层次，不额外改变配色。
  - 让卡片内图表更接近纯趋势线表达，减少“柱状图”既视感。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.62：概览卡加高并改为最近3个月折线瀑布趋势 (2026-04-04)

- **卡片结构调整**：
  - 提高总览概览卡整体高度，让图表区有更充分的展示空间。
  - 将原先贴底的装饰型小图，改成卡片正文内独立展示的趋势图区域。

- **图表样式重做**：
  - 卡片内部微图表改为最近 3 个月的折线瀑布趋势，底部显示月份标签。
  - 保留瀑布块层次，同时增加折线节点与面积填充，让走势更明确、更接近参考图。

- **实现方式**：
  - 在 `web/src/features/dashboard/components/themes/DefaultDashboard.tsx` 中重构概览卡趋势数据结构，改为月份 + 数值组合。
  - 微图表 SVG 组件同步升级为三个月趋势视图，兼顾蓝卡、绿卡和白卡的不同配色层次。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.61：总览概览卡补充瀑布感微型图表 (2026-04-04)

- **界面增强**：
  - 在总览页四张概览卡内部加入瀑布感微型图表，让卡片本身也有数据走势层次。
  - 蓝卡、绿卡与白卡分别沿用当前主题色做轻量曲线和阶梯块，不额外引入突兀的新颜色。

- **实现方式**：
  - 在 `web/src/features/dashboard/components/themes/DefaultDashboard.tsx` 中为概览卡补充微型图表数据生成逻辑。
  - 新增卡片内联 SVG 微图表组件，用最小结构实现瀑布块 + 折线叠加的效果，避免引入额外图表依赖和布局负担。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.60：总览页改为蓝绿色轻量工作台并收起顶部冗长描述 (2026-04-04)

- **开发范围评估**：
  - 本轮属于中等规模的后台界面重构，继续沿用现有 `docs/开发进度.md`、`CHANGELOG.md` 与 `历史版本/` 记录链路即可。
  - 不额外拆分新的专项开发文档，避免把一次风格收敛工作拆得过散。

- **界面风格调整**：
  - 后台桌面端布局改为纯白侧边栏 + 浅灰主工作区，去掉顶部整块外边框包裹感。
  - 顶栏改成右上角轻量工具区，只保留搜索、通知与账户入口的结构。
  - 默认主题与总览模块强调色收敛为蓝绿双主色，整体更接近参考图的轻盈工作台气质。

- **总览页重做**：
  - 取消原先占比过大的说明型 Hero，改成图表卡片 + 指标卡 + 交易表的仪表盘结构。
  - 顶部首屏改为趋势图、收支结构和消费占比组合，中段改为四张概览卡，底部保留关键指标与近期流水。

- **描述文案收敛**：
  - 统一移除后台页面顶部 `ThemeSectionHeader` 的大段说明文本，仅保留必要标题和操作区。
  - 侧边栏同步去掉每个菜单下方的描述性 caption，避免页面入口区出现过多解释文字。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.59：修复消费页资金流向与交易明细区损坏 JSX 文本 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 在资金流向、24 小时消费散点图、金额分布和交易明细区仍有乱码文案。
  - 被污染的标题和说明文本继续破坏 JSX 标签结构，导致新的 `Unexpected token` 构建错误。

- **修复方式**：
  - 修复资金流向说明、散点图标题、金额分布标题和交易明细区文案。
  - 同时替换会直接污染闭合标签的损坏文本，避免该区域继续阻断构建。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.58：修复消费页商户集中与分析卡片区损坏 JSX 文本 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 在商户集中、平台热力图和分析卡片区仍有一批损坏文案。
  - 这些乱码文本会继续破坏 JSX 标签结构，导致新的 `Unexpected token` 构建错误。

- **修复方式**：
  - 修复商户集中标题、平台热力图说明、分析卡片视图文案与结构图谱分组标题。
  - 同步修复对应空状态说明，避免继续出现闭合标签被污染的问题。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.57：修复消费页图表分析区损坏模板字符串与 JSX 文案 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 在图表分析区仍残留多处损坏字符串。
  - 问题分布在浓度镜像图标签、预算分析区标题、备注洞察区说明以及热力日历描述中，会继续触发未闭合字符串和 JSX 结构错误。

- **修复方式**：
  - 修复浓度镜像图与集中度图中的标签数组文本。
  - 修复首页 Hero 提示、预算分析、备注洞察、热力日历等区块中会直接破坏语法的中文文案。
  - 保留业务逻辑不变，只清理会阻断构建的损坏字符串。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.56：修复消费页筛选浮层损坏 JSX 文本 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 的筛选浮层标题和说明文案存在损坏字符。
  - 损坏文本破坏了 JSX 标签结构，导致 Turbopack 报 `Unexpected token`。

- **修复方式**：
  - 修复筛选浮层中的标题、副标题、平台、时间、范围提示等文本。
  - 将当前会直接破坏 JSX 结构的乱码文本替换为正常中文文案。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.55：修复消费页自定义时间筛选区未闭合字符串 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 的自定义时间筛选区存在多处损坏中文文本。
  - 其中年份输入框的 `placeholder` 被破坏，直接导致 `Unterminated string constant` 构建错误。

- **修复方式**：
  - 修复自定义时间按钮文本、年份占位文案、月份选项和年月步进器的无效字符串。
  - 同时整理该区域缩进结构，避免后续再次引入语法噪音。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.54：修复消费页懒加载图表组件残留语法错误 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 中 `LazyChart` 组件后方残留了一段重复的 `return` 代码块。
  - 该残片脱离函数体，导致 Turbopack 报 `Return statement is not allowed here`。

- **修复方式**：
  - 删除 `LazyChart` 内未完成的 `motion.div` 包装残片与后方重复的 JSX 返回代码。
  - 保留可正常工作的懒加载图表实现，恢复组件语法完整性。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.53：修复消费页主题组件非法 import 构建错误 (2026-04-03)

- **问题定位**：
  - `web/src/features/consumption/components/ConsumptionDefaultTheme.tsx` 的 import 行被误写入了转义引号和 `` `n `` 字符串。
  - 导致 Turbopack 在解析消费页组件时直接报 `Expected unicode escape`，阻断消费页编译。

- **修复方式**：
  - 将异常 import 行拆回两条正常的 ES Module 导入语句。
  - 同步扫描 `web/src` 下同类转义串，未发现第二处同类污染。

- **验收结果**：
  - `npm run build` 已通过。

### V2.3.52：修复 3000 端口页面源码编码损坏与运行时异常 (2026-04-03)

- **问题定位**：
  - 3000 端口实际报错并不是主题逻辑本身，而是 `web` 前端多个源码文件存在损坏字符，导致 Turbopack 解析 `globals.css`、`Header.tsx`、`DefaultDashboard.tsx`、`layout.tsx` 时连续抛出 UTF-8 读取错误。
  - 旧的 Next 进程还持续占用 3000 端口，导致页面一直停留在报错实例上。

- **修复方式**：
  - 修复 `web/src/app/globals.css` 中损坏注释。
  - 修复 `web/src/components/shared/Header.tsx`、`web/src/features/dashboard/components/themes/DefaultDashboard.tsx`、`web/src/app/(dashboard)/layout.tsx` 中的损坏文本和异常 JSX。
  - 清理 3000 端口旧进程后，重新启动 `npm run dev:web`。

- **验收结果**：
  - `http://localhost:3000` 已恢复可访问。
  - 浏览器验证已正常进入登录页，不再出现 `useInsertionEffect` 错误页。

### V2.3.51：修复 useInsertionEffect 运行时异常 (2026-04-03)

- **问题定位**：
  - 根目录旧版 Next 入口中的 dashboard 模板仍引用 `framer-motion`，是最可能触发 `Cannot read properties of null (reading 'useInsertionEffect')` 的旧入口点。
  - 现行 `web/` 前端未使用该依赖，问题集中在仓库根目录保留的旧版页面模板。

- **修复方式**：
  - 移除 `src/app/(dashboard)/template.tsx` 中的 `framer-motion` 包装，改回纯容器节点。
  - 保留模板的布局职责，避免旧入口误启动时在客户端初始化阶段触发 React dispatcher 空值错误。

- **验收结果**：
  - `npx next build` 已通过。
  - 根目录 `npx next dev -p 3010` 已可启动，浏览器访问首页未再出现对应报错。

### V2.3.50：默认主题改为更现代的冷白清爽配色 (2026-04-03)

- **配色方向纠偏**：
  - 将上一版偏暖白、偏土色的默认主题进一步调整为冷白、浅雾灰、靛蓝点缀的现代方案。
  - 主题整体从“温和复古感”切回“清爽、年轻、轻专业”的视觉方向。

- **首页强调色重做**：
  - 总览页的图表、收支卡、交易列表与统计标记同步改为靛蓝、薄荷绿、珊瑚橙、浅紫等更年轻的低饱和强调色。
  - 保留简约布局，不再使用偏旧、偏复古的米棕土黄体系。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.49：默认主题重做为暖白简约风并统一首页模块视觉 (2026-04-03)

- **默认主题重做**：
  - 将默认主题从冷蓝科技感切换为暖白、米灰、蓝灰点缀的简约配色，整体更柔和、更耐看。
  - 全局主题变量与主题注册表同步更新，默认进入页面即可看到新的基础视觉。

- **共享样式收敛**：
  - 收紧共享 Surface、Hero、Metric Card 的圆角与交互阴影，去掉装饰性毛玻璃与过强悬浮感。
  - 统一侧边栏、顶部栏、移动底部导航的浅色层次与激活态语言，减少“通用后台模板感”。

- **首页仪表盘调整**：
  - 总览页 Hero、收支概览、现金流图表、消费构成和最近交易改用低饱和暖色体系。
  - 首页保留层次感和重点信息，但不再依赖青蓝色和玻璃感来做视觉记忆点。

- **验收结果**：
  - `npm run typecheck` 已通过。
  - `npm run build` 已通过。

### V2.3.48：根目录 npm 脚本恢复指向旧版 web 项目 (2026-04-01)

- **根目录脚本改回旧站入口**：
  - package.json 中 dev:web 已改为先进入 web 再执行 next dev，不再从仓库根目录直接启动错误的前端入口。
  - build、start、lint、typecheck、test:e2e 与 test:e2e:ui 也同步改为走 web，让根目录 npm 命令重新对应旧版项目。

- **兼容旧工作流**：
  - dev 和 dev:legacy 都继续保留原来的根目录启动习惯，但内部已经改成正确代理到 web。

- **验收结果**：
  - 根目录执行 npm run typecheck 已恢复为走 web 的校验链路。
### V2.3.47：清理已废弃前端迁移记录与临时远端分支 (2026-04-01)

- **记录与文档清理**：
  - 删除仓库内已废弃的专项开发文档与对应历史版本记录。
  - 从 CHANGELOG.md 和 docs/开发进度.md 中移除对应阶段条目，避免继续参考无效方案。

- **远端分支清理**：
  - 删除对应的远端临时分支，避免 GitHub 保留已废弃的试验性入口。

### V2.3.29：页面切换预热与主页面切换性能优化 (2026-03-31)

- **页面切换主链路提速**：
  - 新增 `DashboardRouteWarmup` 空闲预热组件，登录后会按优先级预取总览、消费、资产、储蓄、贷款和数据页路由资源，把等待前移到空闲时段。
  - 预热阶段会根据网络状况控制范围，弱网只保留高优先级路由，避免为了切页提速反而压垮当前网络。
- **整页动态加载收窄**：
  - 总览、资产、储蓄、贷款页面取消整页主题级 `dynamic(..., { ssr: false })`，改为直接进入路由包，避免点击后再补拉整页 UI 代码。
  - 消费和贷款页继续保留图表库按需块，减少首屏路由体积，同时不再把整页主题切换阻塞在运行时动态加载上。
- **主页面热缓存接入**：
  - 新增通用 `warm-cache` 与页面级 data loader，为总览、消费、资产、储蓄、贷款提供短时热缓存与空闲预取，常用页面切换时可直接复用最近一次数据结果。
  - 前端构建校验通过，确认这一轮切换性能优化没有引入新的 TypeScript 或构建错误。

### V2.3.23：数据页工作区重排、AI 页面交互收口与动效优化 (2026-03-31)

- **数据页布局重排**：
  - 顶部改为“总记录数 + 账单导入”同排展示，去掉重复统计卡片。
  - PC 端进一步整理为双列工作区：左侧放总记录数与手动补录，右侧放账单导入与自动归类，整体阅读顺序更稳定。

- **AI 页面交互收口**：
  - 顶部导航栏直接显示“AI 模型配置”，移除页面内部重复的标题 Hero。
  - 已配置模型里的齿轮菜单修复裁切与换行问题，配置弹层补齐滚动区域，避免内容被截断。

- **过渡动效优化**：
  - 底部滑出页统一改为更顺滑的上滑进入和下滑退出。
  - 消费页 AI 分析卡在不同状态之间补齐渐入渐出切换，减少页面硬切换感。

### V2.3.22：规则备注收口、明细列表统一与消费页细节修正 (2026-03-30)

- **规则备注分析收口**：
  - 将消费页“规则备注”聚合结果收敛为单张分析卡片，统一展示备注覆盖统计、金额分布和 Top 备注排行。
  - 规则备注图表优先按自动归类规则里的备注或规则名聚合，不再混入原始订单描述。

- **跨页面明细列表统一**：
  - 继续统一消费页、总览页等交易明细模块的标题字号和紧凑行样式，减少不同页面间的阅读跳变。
  - 消费页底部流水改为仅展示最近 5 条，时间显示收敛为本地短格式，移动端列表更紧凑。

- **消费页细节修正**：
  - 修复消费日历月视图后缀乱码问题。
  - 补齐消费页细节阅读性调整，减少长时间串和大标题对列表空间的挤压。

### V2.3.21：订单号强约束导入、规则备注分析与非 Docker 自动部署 (2026-03-30)

- **账单导入与去重收紧**：
  - 微信账单统一按“交易单号”、支付宝账单统一按“交易订单号”识别 `orderId`，缺少订单号的记录直接视为无效行。
  - 导入阶段继续按 `orderId` 跳过数据库中已有记录和同一批文件内的重复订单，保证同一订单不会重复入库。

- **数据页与消费页联动增强**：
  - 抽出统一交易分类目录 hook，数据页手动补录、批量改分类、自动归类规则和导入完成后都会刷新分类建议。
  - 消费页新增规则备注聚合图、备注覆盖统计和“只看有备注”筛选，便于从固定场景维度复盘支出。
  - 消费页交易明细时间收敛为 `MM-DD HH:mm` 短格式，列表在移动端更紧凑。

- **部署与稳定性补齐**：
  - 新增非 Docker 自动部署工作流、Linux/Windows 部署脚本和配套开发文档，打通 `push main -> 远程构建部署 -> 健康检查` 链路。
  - 补齐 AI 模型配置的内存模式 CRUD，修复无数据库时模型配置页创建直接 500 的问题。
  - 修正总览页总资产重复叠加已同步储蓄、资产页与储蓄页空数据误回退 mock 的问题。

### V2.3.18：消费分析面板增强、图表布局优化与标签防裁切修复 (2026-03-29)

- **消费分析增强**：
  - 为消费页新增更多分析维度，并将分析区收敛为结构视图、复购与集中度、预算与风险、场景偏好四组图表面板。
  - 扩展 dashboard 聚合结果与 mock 数据，保证真实数据和演示数据都能展示新分析维度。

- **图表与明细体验优化**：
  - 统一消费图表在移动端与 PC 端的阅读结构，继续收敛平台列表、热区矩阵、日历和标题区样式。
  - 修复消费交易明细状态乱码，以及预算偏差、大额预警、时段热点、周末偏好等条形图标签裁切问题。

### V2.3.17：模块主题分色与去毛玻璃主题收敛 (2026-03-28)

- **模块主题分色**：
  - 在共享主题原语中补充模块级颜色变量，为仪表盘、消费、资产、贷款、储蓄五个核心模块建立独立色板。
  - 同步调整主操作按钮、进度条、徽标与重点强调色，让各业务模块首屏层次更容易区分。

- **主题去毛玻璃化**：
  - 默认主题和主题注册表切回更扎实的实色容器，降低半透明背景、模糊叠层和玻璃感表面。
  - 共享 `ThemeHero / ThemeSurface / ThemeMetricCard` 改为纯色底和清晰边框，桌面筛选浮层与装饰性模糊一并收掉。

### V2.3.12：主题系统收尾、验收新主题并打通全量校验 (2026-03-24)

- **主题系统收尾**：
  - 继续把 AI、资产、预算、贷款、设置、登录、注册、数据管理、连接等页面的表单区、提示区和操作区接到 shared primitive。
  - 在 `theme-primitives` 里补齐输入框、下拉、多行输入、白底按钮、图标按钮、列表项和状态壳子等共享常量。

- **新增主题验收**：
  - 新增 `terracotta`（赤陶主题）作为全新主题样本。
  - 主题中心自动出现新主题，说明新增主题时不需要逐页改代码。

- **校验链路补齐**：
  - 清理了 `ClientOnly`、`AuthGate`、`Skeletons`、`dashboard/page.tsx`、`assets/page.tsx`、`savings/page.tsx`、`public/sw.js` 等历史 lint/typecheck 问题。
  - `npm run lint`、`npm run typecheck`、`npm run build` 全部通过。

### V2.3.11：统一总览页资金体检为指标卡样式 (2026-03-24)

- **样式结构统一**：
  - 去掉“资金体检”原先的大面板式容器，不再使用上下反差明显的模块视觉。
  - 将负债占比、预算预警、储蓄净流入三项直接改成与顶部核心指标一致的卡片形态。

- **排版结果调整**：
  - 该组卡片继续保留在“总资产 / 总负债 / 储蓄净流入”下方。
  - 新布局和上方三张卡保持同样的一排三列节奏，整体阅读顺序更统一。

- **验证结果**：
  - `npm run lint -- src/features/dashboard/components/themes/DefaultDashboard.tsx`
  - 文件诊断与 lint 校验通过，未发现本次改动引入的新问题。

### V2.3.10：调整总览页资金体检模块布局与位置 (2026-03-24)

- **总览页层级重排**：
  - 将“资金体检”模块前移到顶部三张核心指标卡片下方，和总资产、总负债、储蓄净流入形成连续阅读顺序。
  - 保留预算提醒条在体检模块之后，避免打断顶部关键指标的浏览路径。

- **卡片布局优化**：
  - 将资金体检内的三张洞察卡改为在中等及以上屏幕一行三列展示。
  - 小屏仍保持自适应堆叠，避免内容压缩导致信息难读。

- **验证结果**：
  - `npm run lint`
  - lint 校验通过，说明本次布局调整未引入前端静态检查错误。

### V2.3.7：全局主题系统与 dashboard 页面主题统一 (2026-03-23)

- **全局主题系统落地**：
  - 新增 `web/src/themes/registry.ts`，集中管理主题定义、预览信息与主题变量。
  - 新增 `web/src/components/shared/theme-provider.tsx`，支持主题状态管理、本地持久化与根级别主题注入。
  - 新增 `web/src/components/shared/theme-primitives.tsx`，统一抽象 `ThemeHero`、`ThemeSurface`、`ThemeDarkPanel`、`ThemeSectionHeader`、`ThemeMetricCard` 等共享主题组件。
  - 主题中心页升级为真正的全局主题切换入口，后续新增主题只需要补一份配置，不再逐页重写样式。

- **Dashboard 区域页面完成主题统一接入**：
  - 已接入页面包括：总览、资产、消费、储蓄、贷款、设置、连接、数据管理、后台管理、关于、AI 模型配置、主题中心。
  - `Header`、`Sidebar`、`Dashboard Layout` 也已改为读取统一主题变量，导航与内容区能够一起随主题切换。
  - 这意味着 dashboard 主区域已基本形成“新增主题可全局切换”的能力基础。

- **管理类页面与说明类页面补齐统一风格**：
  - 重构 `settings`、`loans`、`savings`、`data`、`admin`、`about`、`ai` 页面，让它们从原本各自维护样式，切换为共享主题 primitive 组合。
  - 保留原有业务操作能力（如 AI 模型配置、数据导入、批量操作、储蓄目标图片管理、贷款还款计划等），仅统一壳层、指标卡与区块头部结构。

- **验证结果**：
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`
  - 上述校验均通过，说明主题系统与页面迁移后的项目保持可构建状态。

### V2.3.3：修复账单导入重复数据统计Bug (2026-03-22)

- **导入统计准确性提升**：
  - **问题分析**：在执行支付宝/微信账单导入时，如果数据在数据库中已存在（由全局唯一的 `orderId` 决定），`Prisma.createMany` 会自动静默跳过重复数据并返回实际插入条数 0，但这部分未被 `duplicateCount` 统计，导致前端显示“重复 0 条”。
  - **修复方案**：去除了按 `accountId` 过滤的重复性校验，直接对全局唯一字段 `orderId` 进行全局比对；同时，将 `Prisma` 因重复约束自动跳过的数据量（`dbSkippedCount`）准确累加到 `duplicateCount` 之中。
  - **效果**：再次导入已存在的账单时，不再提示迷惑性的“成功 0 条，重复 0 条”，而是准确提示实际的重复条数（例如“重复 158 条”）。

### V2.3.0：多账户权限系统 (2026-03-21)

- **数据库层重构**：
  - 新增 `account_member` 表 - 成员关系与权限（OWNER/ADMIN/MEMBER 三种角色）
  - 所有业务表新增 `accountId` 字段，实现数据隔离
  - 用户 `defaultAccountId` 字段关联默认账户
  - `canViewOwn`/`canManageOwn`/`canViewAll`/`canManageAll` 细粒度权限控制

- **后端改造**：
  - 新增 `requireAccountId` 中间件，自动获取用户默认账户
  - 导入交易 API (`POST /api/transactions/import`) 使用 accountId 隔离数据
  - 内存模式同步支持 accountId

- **一键建表脚本**：
  - `npm run db:create` - Node.js 脚本直接创建数据库表
  - `npm run init:db` - Prisma 模式初始化（含默认账户创建）

- **新增文件**：
  - `docs/多账户权限系统设计.md` - 完整设计文档
  - `docs/完整数据库结构.sql` - 数据库结构 SQL
  - `src/server/scripts/create-tables.ts` - 一键建表脚本

### V2.3.1：数据管理与账户管理功能 (2026-03-21)

- **数据管理页面**：
  - 新增 `/data` 路由，侧边栏添加"数据管理"入口
  - 支持批量选择交易记录（复选框全选/单选）
  - 批量删除交易记录（`POST /api/transactions/batch`）
  - 批量修改交易分类
  - 表格展示交易详情（日期、类型、金额、分类、平台、商户、备注）

- **账户管理功能**：
  - 设置页面新增"账户管理"模块（三列布局之一）
  - 创建新账户（`POST /api/accounts`）
  - 查看账户列表及角色（`GET /api/accounts`）
  - 设置默认账户（`PUT /api/accounts/:id/default`）
  - 账户角色标识（所有者/成员）

- **后端 API 新增**：
  - `POST /api/accounts` - 创建账户
  - `GET /api/accounts` - 获取账户列表
  - `PUT /api/accounts/:id/default` - 设置默认账户

- **TypeScript 构建修复**：
  - 修复 Prisma groupBy/aggregate 类型不匹配
  - 修正模型命名（exchangeRate -> exchangerate, savingsgoal 关系）
  - 修复 _count 选择器字段名称

### V2.3.2：账单导入优化 - 统一分类 (2026-03-21)

- **问题**：微信/支付宝账单前面有多余行，列名不统一，分类和状态类型各异

- **解决方案**：
  - 后端动态识别列名行（扫描前80行，找到特征列匹配）
  - 微信/支付宝分别识别（微信需4个特征匹配，支付宝需3个）
  - 统一分类：18种标准分类（餐饮美食、生活服务、转账红包等）
  - 统一状态：SUCCESS / FAILED / REFUND

- **微信交易类型 → 统一分类映射**：
  - 商户消费/扫二维码 → 日用百货
  - 转账/红包 → 转账红包
  - 信用卡还款 → 信用卡还款
  - 城市服务 → 生活服务
  - 生活缴费 → 充值缴费
  - 医疗健康/交通出行/服饰装扮/数码电器/爱车养车/家居家装 → 对应分类
  - 其他 → 其他

- **状态映射**：
  - 支付成功/对方已收钱/已到账/交易成功 → SUCCESS
  - 转账失败/交易关闭 → FAILED
  - 退款成功 → REFUND

- **平台字段**：
  - 微信 → "微信支付"
  - 支付宝 → "支付宝"

- **修改文件**：
  - `src/server/src/etl/importCsv.ts` - 改进列名识别
  - `src/server/src/etl/mapTransaction.ts` - 添加分类/状态映射

### V2.2.6：重构加载状态与彻底解决滚动恢复 (2026-03-20)

- **问题复盘**：发现即使用 `min-h-screen` 撑起了高度，并且在组件内补全了骨架屏，由于 `page.tsx` 中使用了 Client Component 的 `useState` 和 `useEffect` 控制 `loading` 状态，页面在刷新瞬间依然会有一帧空白（此时 React 还没完成初次渲染与状态初始化），这极短的空白足以让浏览器的 Scroll Restoration 机制判断页面高度为 0，从而强制滚回顶部。
- **最佳实践重构**：
  - 将原本写死在 `consumption/page.tsx` 里的 `SkeletonLoading` 组件提取到了标准的 Next.js App Router 约定文件 `app/(dashboard)/consumption/loading.tsx` 中。
  - **核心原理**：`loading.tsx` 是由 Next.js 服务端直接注入并随初始 HTML 返回的（基于 React Suspense），因此浏览器在收到 HTML 解析的那一刻，页面就已经是撑满骨架屏的完整高度，完美契合了原生浏览器的滚动恢复机制。
  - 移除了上一版本临时加在各个容器上的 `min-h-screen` hack 写法，回归最纯粹的流式渲染架构。

### V2.2.5：彻底修复刷新滚动位置丢失问题 (2026-03-20)

- **根因分析**：虽然 V2.2.4 补全了所有骨架屏组件，但由于 `next/dynamic` 的异步渲染机制以及页面最外层容器（`page.tsx` 和 `ConsumptionDefaultTheme.tsx`）缺乏最小高度限制，在浏览器尝试恢复滚动位置（Scroll Restoration）的瞬间，DOM 的高度依然是 0 或不够，导致滚动条被强制重置到了顶部。
- **解决方案**：
  - 为 `consumption/page.tsx` 的最外层容器添加了 `min-h-screen` 类。
  - 为 `SkeletonLoading` 组件的最外层容器添加了 `min-h-screen` 类。
  - 为 `ConsumptionDefaultTheme` 的主内容区容器添加了 `min-h-screen` 类。
- 确保在 React hydration 和数据请求的任何阶段，页面都有足够的高度支撑浏览器的滚动条恢复。

### V2.2.4：补全底层图表骨架与滚动位置恢复 (2026-03-20)

- **骨架屏高度对齐完整页面**：
  - 发现问题：之前的骨架屏只渲染了前 3 行图表和底部表格，导致总高度远低于真实页面。当用户在页面底部刷新时，由于骨架屏高度不够，浏览器无法恢复正确的滚动位置，只能显示顶部的骨架。
  - 解决方案：在 `consumption/page.tsx` 的骨架屏中补全了剩余的图表占位：
    - Row 4: 帕累托图 & 消费日历
    - Row 5: 热力分布 & 每日平均消费
    - Row 6: 桑基图 (全宽大图占位，适配了桌面端 450px 与移动端 300px 的高度)
    - Row 7: 散点图 & 直方图
  - 现在骨架屏的总高度与完全加载后的页面高度基本一致，完美支持浏览器在刷新时保持用户的滑动位置。

### V2.2.3：骨架屏样式像素级深度还原 (2026-03-20)

- **核心数据卡片优化**：
  - 调整 `StatsCardSkeleton` 的高度，使其与真实卡片的紧凑高度 (`min-h-[80px] sm:min-h-[45px]`) 保持一致。
  - 精简卡片内部的占位条数量和高度，更加贴近单行大数字的真实视觉。
- **图表区骨架拆分与重构**：
  - 弃用全屏大圆形的统一 `ChartSkeleton`，因为它会导致折线图和柱状图区域显得极不自然。
  - 新增 `PieChartSkeleton` 组件专门用于“支付平台分布”和“收支分析”等饼图。
  - 重构 `ChartSkeleton`，改为更通用的底部对齐的柱状条占位（带有随机高度），适用于条形图、折线图和堆叠图。
- **色彩与细节打磨**：
  - 顶部 AI 相关占位符增加符合真实按钮的浅蓝色背景（`bg-blue-50/50` 和 `bg-blue-500/20`）。
  - 微调各个图表区域的高度 (`md:min-h-[280px]`) 以精确匹配真实 ECharts 容器。

### V2.2.2：修复消费页面布局偏移问题 (2026-03-20)

- **骨架屏结构对齐**：
  - 更新 `consumption/page.tsx` 中的 `SkeletonLoading`，补全了顶部标题、AI分析卡片占位以及筛选栏的骨架。
  - 将图表骨架屏从单行全宽调整为与真实页面完全一致的 3 行网格布局（支持 `col-span-2` 等精确占位）。
- **AI 智能分析卡片修复**：
  - 修复 `AIAnalysisCard` 组件在 `compact` 模式下初始状态渲染高度错误的 Bug，确保其在未分析前保持长条按钮形态，防止真实组件加载时出现高度突变。

### V2.2.1：前端加载体验统一优化与骨架屏升级 (2026-03-20)

- **前端加载体验统一优化**：
  - 移除了所有页面（首页、消费、资产、贷款、后台管理）在组件懒加载期间的生硬转圈 Loading 图标。
  - 将所有懒加载占位统一替换为与对应页面布局1:1匹配的 **骨架屏 (Skeleton)** 动画。
  - 现在用户刷新页面时，只需看到一次居中的"正在验证登录状态"，随后页面会立刻以骨架屏形式展现基础结构并平滑过渡到真实数据，彻底消除了多次不同 Loading 样式带来的视觉割裂感。

### V2.2.0：消费页面移动端优化与 AI 智能分析 (2026-03-20)

- **AI 智能分析功能上线**：
  - 新增 `POST /api/ai/analyze-consumption` 后端接口
  - 创建 `analyzeConsumption` AI 分析服务，支持消费数据智能分析
  - 新增 `AIAnalysisCard` 前端组件，展示分析结果
  - AI 分析包括：消费概览总结、关键洞察、优化建议
  - 支持按优先级（高/中/低）展示建议卡片
  - 洞察类型支持：info/warning/success

- **AI 分析交互效果优化**：
  - 骨架屏动态加载效果：Stats 数据格、图表区、洞察卡片均有独立骨架动画
  - 分析报告打字机效果：Summary 文字逐字显示，带闪烁光标
  - 洞察和建议逐条弹出：每 300ms 逐条显示一条，带 slide-in 动画
  - Loading 时显示旋转 Loader 图标，实时反馈分析进度
  - 修复打字阶段转换 bug：Summary 完成后正确进入 insights 和 suggestions 阶段

- **移动端图表优化**：
  - 消费页面「每日平均消费 (按周)」图表在移动端支持 X 轴标签 45 度旋转显示，解决小屏幕下标签重叠问题
  - PC 端保持正常角度显示，确保可读性
  - 根据 `isMobile` 状态动态调整 `rotate` 和 `bottom` 参数

- **构建错误修复**：
  - 修复 `ConsumptionDefaultTheme.tsx` 中 `<DelayedRender>` 标签未正确闭合的 JSX 解析错误
  - 修复 `ai/page.tsx` 中 Button 组件使用不存在的 `asChild` prop 问题（base-ui 版本不支持）
  - 修复 `page.tsx` 中 Transaction 类型缺少 `description` 字段的问题
  - 修复 `notifications.ts` 中 `actions` 和 `vibrate` 属性不存在于 `NotificationOptions` 的类型错误
  - 修复 `echarts-for-react` v3.x 移除 `ref` prop 导致的所有图表引用报错，移除手动 resize 处理逻辑
  - 安装缺失的 `@playwright/test` 依赖

---

### V2.1.8：AI 识别平台选择与多平台支持 (2026-03-19)

- **平台选择功能**：在消费页 AI 记账功能中新增平台选择下拉框，支持切换支付宝、微信支付、云闪付三个平台。
- **平台特定字段**：不同平台识别结果展示不同字段：
  - 云闪付：消费名称、卡号、交易时间、交易类别
  - 微信：商品描述、商户全称、交易时间
  - 支付宝：账单分类、付款方式、收款方全称、支付时间、备注
- **AI Prompt 优化**：为每个平台定制独立的识别提示词，确保模型准确识别各平台账单格式。

---



### V2.1.7：AI 记账时间提取与展示优化 (2026-03-19)

- **表单化简与合并**：针对 AI 提取结果中同时存在 `date` 和 `paymentTime` 导致的冗余问题，移除了前端弹窗底部的单独「支付时间」输入框。
- **时间精度升级**：将顶部的「日期」标签修改为「支付时间」，并将输入框类型从 `date` 升级为 `datetime-local`，支持精确到秒的时间显示和编辑。
- **数据格式化处理**：在接收到 AI 识别结果时，优先采用精确的 `paymentTime` 并自动格式化为 `YYYY-MM-DDThh:mm:ss` 填充至表单；在最终保存调用 `/api/transactions` 时，自动截取日期部分 (`YYYY-MM-DD`) 满足后端原有结构要求，既提升了展示精度，又保障了系统兼容性。

---

### V2.1.6：AI 记账提取更多账单详情 (2026-03-19)

- **AI 提取能力增强**：优化了调用豆包视觉模型时的 Prompt，新增对「账单分类」、「付款方式」、「支付时间」、「收款方全称」和「备注」5个关键字段的提取支持。
- **前端表单扩展**：在消费页面的 AI 拍照记账确认弹窗中，增加了这 5 个字段的输入框，供用户查看和微调模型提取的结果。
- **数据留存策略**：由于数据库原有的 Transaction 表结构不包含这些细分字段，在用户确认记账时，将这些额外信息（如存在）智能拼接到交易的 `description` (描述) 字段中进行保存，既保证了数据不丢失，又无需改动底层表结构。

---

### V2.1.5：AI 模型配置表单提示二次优化 (2026-03-19)

- **配置提示更正**：根据火山引擎最新的「快捷接入预置推理服务」规范，修改了前端的模型 ID 填写提示。明确告知用户：不需要强制使用 `ep-` 开头，而是直接复制控制台代码示例中自动生成的模型 ID（如 `doubao-seed-2-0-mini-260215`），避免误将下拉框的显示名称填入。

---

### V2.1.4：AI 模型配置表单提示优化 (2026-03-19)

- **表单交互优化**：在新增/编辑和配置大模型表单中，动态检测提供商是否为火山引擎。
- **配置提示增强**：当用户选择火山引擎（豆包）时，提供红色提示信息，明确要求「模型 ID」字段必须填写以 `ep-` 开头的「接入点 ID」，避免因直接填入模型名称（如 `Doubao-Seed-2.0-mini`）而导致的 404 错误。

---

### V2.1.3：大模型页面功能完善 (2026-03-19)

- **API 端口修复**：创建 `web/.env.local` 文件，将前端 API 请求端口从错误的 3006 修正为正确的 3004，解决 AI 记账功能无法调用后端的问题。
- **设为默认模型功能**：在大模型管理页面添加"设为默认"选项，用户可将常用模型设为默认，AI 记账时优先使用默认模型。
- **AI 识别交互优化**：上传图片后不再自动识别，改为显示"开始识别"按钮，用户主动点击后才触发 AI 识别。
- **AI 识别错误提示优化**：改进消费页面 AI 识别的错误处理，针对未配置 API Key、未登录等场景提供更友好的错误提示。

---

### V2.1.2：顶部导航栏与鉴权路由修复 (2026-03-18)

- **导航栏展示优化**：修复 `Header` 组件，加入 `loading` 状态，防止未响应时一直显示静态 "User" 和 "加载中..."。
- **鉴权网关恢复**：重新启用 `AuthGate` 守卫，现在获取用户信息失败或未登录时，将正确显示 loading 并安全跳转到 `/auth/login`。
- **后端降级支持**：为 `/api/auth/me`、`/api/auth/login` 及 `/api/auth/register` 添加无数据库(内存)模式的兼容支持。
- **登录页表单修复**：修复浏览器自动填充账密时可能未触发 `onChange` 导致提交空数据的问题，改用 `FormData` 获取实际值；同时将受控组件改为非受控组件（`defaultValue`），解决登录失败后输入框被清空的问题。

---

### V2.1.1：大模型管理页面与 AI 拍照记账 (2026-03-18)

- **大模型管理页面 (/ai)**：
  - 新增侧边栏"大模型"菜单入口
  - 创建 `web/src/app/(dashboard)/ai/page.tsx` 页面
  - 支持添加、编辑、删除自定义大模型配置
  - 支持配置 API Key、提供商、端点、模型 ID
  - 已配置/未配置模型分组展示

- **消费页面 AI 记账**：
  - 在消费分析页面添加"AI 记账"按钮
  - 实现图片上传与 AI 识别流程
  - 弹出 BottomSheet 展示识别结果
  - 用户可修改金额、商户、日期、分类后确认记账
  - 识别成功后自动创建交易记录

- **统一背景装饰**：
  - 重构 `GridDecoration` 组件，简化为一根底部曲线
  - 将背景装饰移至 dashboard layout，所有页面统一展示
  - 使用 `fixed` 定位固定在底部，不随滚动移动

- **后端 API 完善**：
  - 完善 `POST /api/ai/scan-receipt` 接口
  - 支持 JPEG、PNG、WebP 格式图片（最大 10MB）
  - 调用 doubaoAi 服务进行视觉识别

---

### V2.1.0：AI 智能视觉记账后端基础设施 (2026-03-18)

- **AI 引擎接入**：
  - 集成 `openai` SDK，作为与火山引擎（豆包）模型通信的标准接口。
  - 创建 `src/server/src/services/doubaoAi.ts` 服务模块，封装了图片 Base64 转换、Prompt 构造、JSON 解析与错误处理逻辑。
  - 选用 **Doubao-vision-pro** 模型，专注于中文小票与账单的高精度识别。

- **后端 API 实现**：
  - 新增 `POST /api/ai/scan-receipt` 接口，支持 `multipart/form-data` 图片上传。
  - 实现了基于内存 (`multer.memoryStorage`) 的图片流处理，不占用服务器磁盘空间，保障数据隐私。
  - 接口返回标准化的结构数据（金额、商户、日期、分类、描述），直接对接前端确认弹窗。

- **文档与规范**：
  - 创建 `docs/AI智能记账开发文档.md`，详细定义了从技术选型到接口设计的完整规范。
  - 修复 `CHANGELOG.md` 解析接口的路径问题，支持后端自动化读取版本日志。

---

### V2.0.6：Dashboard 架构重构与视觉风格升级 (2026-03-18)

- **视觉风格终极进化**：
  - **废弃直线装饰**：移除了带有工业感的网格线和直线线条。
  - **流体弯曲线条**：重构了 `GridDecoration` 组件，使用 SVG 贝塞尔曲线绘制了 4 条具有随机流动感、分散感的不规则弯曲线。这种设计风格对标现代高级金融 UI，视觉上更加柔和、轻盈。
  - **抽象层次感**：通过不同的线条粗细（0.3 - 1.2）和虚实结合，在卡片背景中营造出一种深度的、动态的抽象层次感。

- **数据可视化深度优化**：
  - **收支趋势对比**：在统计卡片中引入了微型趋势指示器。
  - **新增消费占比环形图**：在首页引入了 Recharts 环形图。
  - **布局响应式升级**：重构图表展示区为双列展示。

- **代码架构优化**：
  - 抽离视觉组件。
  - 封装 `formatCurrency` 工具函数。

---

### V2.0.5：骨架屏与真实内容高度精准匹配 (2026-03-18)

- **UI 优化**：
  - 彻底解决了储蓄页面底部存取记录模块在加载完成时的轻微高度跳变。
  - 通过调整 `CardListSkeleton` 内部的 padding (`p-6 pb-4`) 和列表项边距，使其与真实数据的 `Card` 容器内部结构及 `CardHeader`、`CardContent` 的边距达到像素级的一致，从而彻底杜绝了高度变化导致的滚动条闪现。

---

### V2.0.4：储蓄页面存取记录模块高度跳变修复 (2026-03-18)

- **UI 优化**：
  - 修复储蓄页面底部“存取记录”模块在刷新时由于未正确包裹 `DelayedRender` 导致瞬间渲染真实数据而产生的局部高度跳变和滚动条闪现问题。
  - 重新为存取记录模块添加了 `DelayedRender` 组件包裹，并配置了 `delay={200}` 以及匹配真实数据高度的 `fallback={<CardListSkeleton count={2} />}`。

---

### V2.0.3：储蓄页面布局抖动终极修复 (2026-03-18)

- **UI 优化**：
  - 彻底解决了储蓄页面在刷新或异步加载时由于内容高度变化导致滚动条突然出现（从而引起页面水平偏移和整体闪烁）的问题。
  - 为 `DefaultSavings.tsx` 的根容器重新添加了 `min-h-[101vh]`，强制页面始终显示垂直滚动条，保持了布局的绝对稳定。

---

### V2.0.2：骨架屏高度跳变修复与架构统一 (2026-03-18)

- **UI 优化**：
  - 修复储蓄页面存取记录模块加载时的视觉闪烁问题。
  - 将储蓄页面（`DefaultSavings.tsx`）的加载架构与消费页面对齐，全面引入 `DelayedRender` 组件，实现图表和表格的渐次入场动画（分别设置 `delay` 为 50ms、100ms、150ms）。
  - 移除页面内冗余的自定义骨架屏，统一复用 `Skeletons.tsx` 中标准的 `ChartSkeleton`、`ListTableSkeleton` 和 `CardListSkeleton`，提升了代码的复用性和界面的统一感。

---

### V2.0.1：界面细节修复 (2026-03-18)

- **UI 优化**：
  - 修复了因为 `html` 标签应用了 `scrollbar-gutter: stable` 而导致浏览器窗口最右侧出现白色滚动条占位宽度的布局问题。
  - 仅保留对主滚动容器的 `.scrollbar-stable` 类的应用，保持内部防抖动效果的同时去除了外部白边。

---

### V1.8.5：系统优化与重构 (2026-03-18)

- **代码复用与工程化**：
  - 抽离 `DelayedRender` 组件至 `web/src/components/shared/DelayedRender.tsx`
  - 统一 Skeleton 组件至 `web/src/components/shared/Skeletons.tsx`（包含 ChartSkeleton、ListTableSkeleton、StatsCardSkeleton、CardListSkeleton）
  - 创建通用 `EmptyState` 组件至 `web/src/components/shared/EmptyState.tsx`
  - 类型定义集中管理至 `web/src/types/index.ts`

- **资产页面优化**：
  - 添加骨架屏加载态
  - 统一页面布局参数（max-w-[1600px]）
  - 支付宝/微信图标替换为 simple-icons 官方品牌矢量图标
  - 添加 EmptyState 空状态展示

- **贷款页面优化**：
  - 添加骨架屏加载态
  - 统一页面布局参数
  - 添加 EmptyState 空状态展示

- **仪表盘优化**：
  - 首页加载体验升级（Spinner 替换为骨架屏）
  - 图表渲染引入 DelayedRender 实现渐次入场动画

- **性能与交互**：
  - 全局应用 `scrollbar-gutter: stable` 消除滚动条显隐造成的页面偏移
  - 移动端适配检查（Assets/Loans 页面 Grid 布局优化）
  - 吸顶筛选栏模式评估（资产/贷款页面无复杂筛选条件，不适用）

---

### V2 阶段一：预算系统与预警增强 (2026-03-17)

- **数据库更新**：
  - Budget 模型新增 `scopeType` 字段（GLOBAL/CATEGORY/PLATFORM），支持多维度预算控制
  - Budget 模型新增 `platform` 字段，支持平台级别预算
  - Budget 模型新增 `alertPercent` 字段（默认 80%），支持自定义预警阈值
  - 新增 `BudgetScope` 枚举类型
  - 更新唯一约束为 `[userId, category, period, scopeType, platform]`

- **后端接口**：
  - 扩展 `/api/budgets` 接口，返回预算健康状态（normal/warning/overdue）
  - 新增 `/api/budgets/alerts` 接口，获取所有预警/超支的预算列表
  - 更新 `budget.ts` 逻辑层，新增 `calculateBudgetHealth` 函数

- **前端展示**：
  - 预算管理页重构，支持选择预算作用域（全局/分类/平台）
  - 预算进度条根据健康状态变色（<80%绿色，80%-100%黄色，>100%红色）
  - 预算卡片显示状态标签（正常/预警/超支）
  - 支持自定义预警阈值设置

- **Dashboard 首页**：
  - 新增预算预警提示卡片，显示所有预警/超支的预算
  - 预警卡片支持跳转到预算管理页
  - 快捷入口新增"预算管理"入口

---

### V1 核心功能（已完成）

- 功能：侧边栏新增"关于"页面，包含项目介绍、版本更新记录、更新版本、贡献者、网站模块
- 优化：消费页体验增强（新增底部悬浮筛选 Popover，修复滚动监听）
- 性能：消费页图表方向性动效重做（去偏移渐显、柱图生长、折线绘制、日历逐格加载）
- 功能：消费页新增资金流向桑基图（Sankey）、时段散点图（Scatter）与金额直方图（Histogram）
- 修复：桑基图节点标签自定义渲染与布局防截断优化
- 性能：消费页加载动效恢复平滑（分批懒加载 + 渐入位移动画 + 延迟节奏重排）
- 性能：消费页图表改为逐个串行加载（渲染步进控制，避免同屏并发渲染）
- 性能：消费页刷新卡顿优化（图表分时挂载、空闲回调调度、热力图 Map 预索引、筛选渲染落地）
- 优化：消费页微信/支付宝图标改为品牌官方矢量（simple-icons）
- 修复：消费页图表刷新时位置抖动（固定容器尺寸，内部骨架加载）
- 优化：消费页图表加载体验优化（骨架屏占位，消除高度跳变）
- 优化：消费页第4行图表网格改为顶部对齐，移除帕累托图底部空白
- 优化：消费日历改为扁平化全展示（无滚动、方块更扁平）
- 优化：消费页顶部四卡图标与平台品牌色统一（微信/支付宝）
- 修复：消费页桑基图布局算法（ECharts layout）导致图形高度未占满容器
- 修复：DelayedRender 过渡容器未继承高度导致 ECharts 100% 高度失效
- 修复：移动端 BottomSheet 滑出/收回动画不连贯（闪烁）的问题
- 修复：储蓄页移动端确认关闭弹窗循环触发（取消后重复弹出）
- 修复：储蓄页确认弹窗取消后的二次关闭事件穿透（增加一次性忽略与弹窗状态锁）
- 性能：全站路由切换性能优化（通过 next/dynamic 异步加载各模块重型图表组件，解决侧边栏切换卡顿）
- 优化：储蓄页增强（新增指定计划、隔月存说明悬浮窗）
- 优化：新建/编辑目标弹窗重构（Shadcn UI风格）
- 优化：储蓄页增强（新增每月/隔月类型、存款性质分类）
- 优化：储蓄页重构（对齐消费页主题、新增分布图表、搜索与懒加载）
- 功能：移动端适配（Header 增加 Hamburger 菜单与 Side Drawer 导航）
- 优化：全站 UI 风格统一（Assets/Savings/Loans）
- 优化：仪表盘首页（Dashboard）移动端响应式布局与字体大小适配
- 优化：消费页过滤器与图表标签优化
- 优化：侧边栏固定与新增 Theme 页面
- 性能：消费页图表懒加载优化（IntersectionObserver）
- 架构：前端 Feature-based 架构重构完成
- 前端：Next.js App Router（web/），包含 Dashboard 路由组与基础布局（Sidebar/Header）
- 前端：Query Provider 已接入（TanStack Query）
- 前端：已接入登录/注册页、Token 鉴权保护与退出登录入口
- 前端：连接管理页已实现生成连接码、倒计时、设备列表与撤销授权
- 前端：消费页已实现账单导入、支出流水列表（分页）与汇总/趋势/分类分析展示
- 前端：消费页新增日期范围筛选、收支类型切换与平台筛选
- 后端：Express（src/server），包含 `/api/health` 与连接域 API
- 后端：已接入 Prisma 并落地核心模型（User/Transaction/Loan/AppConnection 等）
- 后端：账单 ETL（表头识别与字段映射）与交易导入/查询 API 已可用
- 后端：消费分析聚合指标接口已可用（支持日期/收支类型/平台筛选）
- 后端：已接入 JWT 登录/注册接口，并对核心数据接口启用鉴权校验
- 后端：储蓄目标模块已实现 CRUD 接口（支持内存模式与数据库模式）
- 前端：储蓄目标管理页已上线（进度展示、目标增删改）
- 后端：贷款（Loan）模块已实现 CRUD 接口（支持内存模式与数据库模式）
- 前端：贷款管理页已上线（进度展示、还款信息、增删改）
- 前端：贷款管理页新增"还款计划"弹窗（自动推算未来还款日程）
- 前端：消费页新增交易数据 CSV 导出功能
- 后端：资产（Asset）模块已实现 CRUD 接口（支持内存模式与数据库模式）
- 前端：资产管理页已上线（列表展示、总资产估值、增删改）
- 前端：Dashboard 首页上线（聚合资产、负债、本月收支与近期交易）
- 后端：设置模块已实现（修改用户信息/密码）
- 前端：设置页已上线（基本信息修改、密码重置）
- 基础设施：根目录 npm run dev 一键启动前后端（Concurrently）
- 优化：消费图表支持按日/按月切换与 TopN 分类筛选
- 功能：消费页支持交易流水编辑（金额/分类/商家/时间）与删除
- 后端：预算（Budget）模块已实现 CRUD 接口与进度统计
- 前端：预算管理页已上线（列表、进度条、增删改）
- 模块：多币种支持与汇率换算（ExchangeRate）
  - 后端：汇率获取接口与自动换算逻辑（API /api/exchange-rates）
  - 前端：资产页支持多币种显示与统一折算

## 系统优化与重构 (Refactoring)

> 详见 [系统优化清单.md](./系统优化清单.md)

- [x] **代码复用**: 抽离 `DelayedRender`, `Skeleton`, `EmptyState` 等通用组件。
- [x] **页面一致性**: 统一 Assets/Loans 页面的加载骨架屏、图标与布局参数。
- [x] **Dashboard**: 优化首页加载体验与图表入场动画。
- [x] **布局抖动修复**: 全局应用 `scrollbar-gutter: stable` 消除滚动条显隐造成的偏移。

### V2 阶段二：自动化运维与端到端测试 (2026-03-17)

- **CI/CD 流水线**：
  - 配置 `.github/workflows/ci.yml`
  - 支持 Push 后自动 Lint、TypeCheck、Build
  - E2E 测试集成（Playwright）

- **E2E 测试**：
  - 引入 Playwright 测试框架
  - 编写认证流程测试用例（auth.spec.ts）
  - 编写预算管理测试用例（budget.spec.ts）

- **Docker 部署优化**：
  - 添加健康检查（Healthcheck）配置
  - 配置 Nginx 反向代理
  - 支持 HTTP/HTTPS 双端口

---

### V2 阶段三：数据批量操作与异常处理 (2026-03-17)

- **数据库更新**：
  - 新增 `ImportErrorLog` 模型，记录账单导入异常
  - 字段：fileName, lineNumber, rawData, errorMessage, errorType, resolved

- **后端接口**：
  - 新增 `/api/transactions/batch` 接口，支持批量删除和批量修改分类
  - 新增 `/api/import-errors` 接口，获取导入错误日志列表
  - 新增 `/api/import-errors/:id/resolve` 接口，标记错误已处理
  - 新增 `/api/import-errors/:id` DELETE 接口，删除错误记录

---

### V2 阶段四：高级权限与后台管理 (2026-03-17)

- **数据库更新**：
  - User 模型新增 `role` 字段（USER/ADMIN），默认为 USER
  - 新增 `UserRole` 枚举类型

- **后端接口**：
  - 新增 `requireAdmin` 鉴权中间件，验证用户是否为管理员
  - 新增 `/api/admin/stats` 接口，返回系统统计数据
  - 新增 `/api/admin/users` 接口，分页获取用户列表
  - 新增 `/api/admin/users/:id/role` 接口，修改用户角色

- **前端展示**：
  - 新增 `/admin` 路由，后台管理面板
  - 系统统计卡片：用户数、交易数、资产数、贷款数、储蓄目标、预算数
  - 用户列表表格：显示用户信息、角色、交易/资产/预算数量
  - 角色切换功能：管理员可修改用户角色

---

### V2 阶段五：智能化与架构演进 (2026-03-18)

- **NLP 自动分类**：
  - 新增 `nlpClassifier.ts` 服务，基于关键词匹配实现智能分类
  - 支持 100+ 分类关键词规则
  - 支持商家名称识别与别名匹配
  - 账单导入时自动识别分类

- **异常消费检测**：
  - 新增 `anomalyDetector.ts` 服务
  - 支持大额支出检测（默认阈值 ¥1000）
  - 支持高频消费检测（默认 30 天内 10 次）
  - 支持异常消费类别检测（新出现的消费类别）
  - 生成风险账单报告（按严重程度分级）

- **事件总线解耦**：
  - 新增 `eventBus.ts` 服务，实现内存模式事件总线
  - 支持事件发布/订阅模式
  - 支持异步事件处理
  - ETL 流程异步化：导入完成时触发事件
  - 异常检测完成时触发事件

  - 预算预警触发事件

---

### V2 阶段六：移动端体验优化 (2026-03-18)

- **PWA 离线支持**：
  - 新增 `manifest.json` 配置文件
  - 新增 Service Worker (`sw.js`) 支持离线访问
  - 新增离线提示页面 (`offline.html`)
  - 新增 PWA 安装提示组件
  - 支持添加到主屏幕

- **手势操作优化**：
  - 新增 `SwipeToDelete` 组件 - 滑动删除
  - 新增 `PullToRefresh` 组件 - 下拉刷新
  - 新增 `LongPress` 组件 - 长按操作
  - 支持触摸手势优化

- **推送通知集成**：
  - 新增 `notificationService` 服务
  - 支持预算预警推送通知
  - 支持异常消费提醒推送
  - 支持账单导入完成通知
  - 新增 `useNotification` Hook

---

## 待办

- 阶段七：数据可视化增强（规划中）
  - [ ] 更多图表类型
  - [ ] 数据导出增强
  - [ ] 自定义报表

## 环境状态

- **数据库**: MySQL 8.0 (localhost:3306)
- **管理员**: admin@openstar-StarAccounting.com / Admin123.

---

### 2026-03-28 模块主题分色优化

- **需求判断**:
  - 本次属于现有主题系统上的局部视觉增强，不新增独立开发文档。
  - 继续沿用 `docs/主题开发记录.md` 记录主题系统演进细节。

- **本次完成**:
  - 在共享主题原语中补充模块级颜色变量，统一支持模块表面、Hero 区和指标卡的差异化着色。
  - 为仪表盘、消费、资产、贷款、储蓄五个核心模块分别挂载独立色板。
  - 同步调整贷款/储蓄进度条、主操作按钮，以及资产/消费/仪表盘的重点徽标与主操作色。

- **验证结果**:
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行定向 `npm run lint -- ...` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-28 去毛玻璃主题调整

- **反馈来源**:
  - 根据本轮体验反馈，现有主题虽然模块分色更明确，但毛玻璃和半透明容器仍然不符合预期。

- **本次完成**:
  - 将默认主题和主题注册表中的壳层、头部、侧边栏、表面、Hero、指标卡改为更偏实色的层级体系。
  - 共享主题原语去掉模块色叠层渐变，改为纯色容器 + 清晰边框。
  - 移除消费页桌面筛选浮层、Hero 浮卡以及仪表盘等页面里的主要模糊装饰。

- **验证结果**:
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行定向 `npm run lint -- ...` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费图表移动端布局微调

- **需求判断**:
  - 本次为消费页局部响应式布局调整，不新增独立开发文档。

- **本次完成**:
  - 将消费页“支付渠道占比”和“本期收入与支出”两张图表卡片在移动端改为与 PC 一致的左右双列结构。
  - 移动端图表宽高调整为 `84px`，避免占掉右侧说明区域。

- **验证结果**:
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费平台 Logo 细节收敛

- **需求判断**:
  - 本次为消费页局部视觉细节调整，不新增独立开发文档。

- **本次完成**:
  - 去掉消费页平台徽标外层的圆形描边，仅保留淡色底托。
  - 调整范围包括支付宝、微信和默认平台图标容器。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。

### 2026-03-29 消费平台 Logo 底托移除

- **反馈来源**:
  - 上一轮只去掉了平台 Logo 外圈描边，但支付宝和微信图标外层的浅色底托仍然保留。

- **本次完成**:
  - 去掉消费页平台列表中支付宝、微信和默认平台图标外层的浅色圆角底托。
  - 保留固定尺寸占位，避免图标移除背景后列表错位。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费热区矩阵高度压缩

- **需求判断**:
  - 本次为消费页局部信息密度调整，不新增独立开发文档。

- **本次完成**:
  - 压缩“平台 × 分类”热区矩阵的左侧标签列宽。
  - 缩小矩阵行距和单元格纵向 padding，降低整体高度。
  - 保留原有热力层级和数值展示，不改变矩阵信息结构。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费图表标题区收敛

- **需求判断**:
  - 本次为消费页图表头部视觉收敛，不新增独立开发文档。

- **本次完成**:
  - 去掉消费页图表卡片右上角的时间胶囊、统计胶囊和图标按钮。
  - 调整范围包括趋势图、收支对比、商户榜单、热区矩阵、消费日历、周内平均、桑基图等图表区。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费热区矩阵与日历圆角收敛

- **反馈来源**:
  - 热区矩阵和消费日历中的单元格在当前宽度下圆角偏大，移动端尤其容易出现胶囊化、椭圆化观感。

- **本次完成**:
  - 将热区矩阵单元格的移动端圆角从 `rounded-xl` 收到 `rounded-lg`，桌面端从 `rounded-2xl` 收到 `rounded-xl`。
  - 将消费日历月份格、日期格和空白占位格的圆角同步收紧。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费图表列表样式对齐总览页

- **需求判断**:
  - 本次为消费页两个图表模块的版式重构，不新增独立开发文档。

- **本次完成**:
  - 将“平台分布”和“收支对比”改成与总览页“近期消费构成”一致的左图右侧信息行布局。
  - 平台分布右侧改为最多 3 条圆角信息行，保留平台占比说明。
  - 收支对比右侧去掉进度条，改为圆角信息行展示收入和支出金额与占比。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费日历 PC 端字体微调

- **需求判断**:
  - 本次为消费日历局部可读性微调，不新增独立开发文档。

- **本次完成**:
  - 仅提高消费日历在 `sm` 及以上断点中的日期字号和金额字号。
  - 移动端日历字号保持不变，避免影响小屏排版。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费交易明细乱码修复

- **需求判断**:
  - 本次为消费页文案修复，不新增独立开发文档。

- **本次完成**:
  - 修复消费页底部交易明细中状态文案乱码。
  - 将异常显示恢复为正常的“收入 / 支出”文本。

- **验证结果**:
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。

### 2026-03-29 消费分析维度扩展

- **需求判断**:
  - 本次为消费页分析能力增强，涉及前后端和 mock 数据联动，但仍属于现有消费模块的功能扩展，不新增独立开发文档。

- **本次完成**:
  - 扩展消费 dashboard 接口，在原有图表数据基础上新增多组分析维度聚合结果。
  - 新增消费属性、必要/可选、资金性质、高频商户/订阅、预算偏差、高峰场景、周末偏好、大额预警、消费集中度等模块。
  - 同步更新消费页类型定义和 mock 数据，保证真实数据与演示数据都能展示新维度。

- **验证结果**:
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行定向 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/shared/mockData.ts` 通过。
  - `src/server/` 目录执行 `npm run build` 通过。
  - 仓库根目录执行 `npm run build` 通过。

### 2026-03-29 消费分析维度图表化收敛

- **需求判断**:
  - 本次为消费页分析区表现形式调整，不新增独立开发文档。

- **本次完成**:
  - 将“更多分析维度”从多张信息卡重构为四张图表卡。
  - 新布局分别承载结构视图、复购与集中度、预算与风险、场景偏好。
  - 保留已有分析结果数据，不再继续平铺大量文本型模块。

- **验证结果**:
  - `web/` 目录执行 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/shared/mockData.ts` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 消费分析条形图标签防裁切

- **反馈来源**:
  - 预算与风险、场景偏好中的条形图在右侧显示数值标签时出现超出容器的问题。

- **本次完成**:
  - 增大预算偏差、大额预警、时段热点图表的右侧安全留白。
  - 提高周末偏好图表的右侧留白，并将 X 轴最大值从 `100` 放宽到 `110`，给百分比标签预留空间。

- **验证结果**:
  - `web/` 目录执行 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/shared/mockData.ts` 通过。
  - `web/` 目录执行全量 `npm run typecheck` / `npm run build` 时，被工作区里与本次无关的 `SavingsGoalDialog.tsx`、`SavingsPlanDialog.tsx` 语法错误阻塞。
### V2.3.16：交易对方自动归类、AI 模型配置修复与 AI 页面概览调整 (2026-03-29)

- **交易对方自动归类**：
  - 数据页新增交易对方搜索、去重列表和自动归类规则面板。
  - 支持按“交易对方 -> 分类 / 备注”建立规则，并可选择回填历史交易。

- **入库流程自动套用规则**：
  - 手动新增交易、账单导入和 APP 同步推送都会按交易对方规则自动设置分类。
  - 固定支出对象后续再次出现时，无需重复手动调整消费分类。

- **AI 模型配置修复**：
  - AI 页面创建、列表、更新、删除与默认模型读取统一切到当前账户作用域。
  - 修复 `accountId` 写入错误导致“创建失败”的问题。

- **AI 页面概览区布局**：
  - “模型总数 / 已配置 / 未配置 / 功能说明” 四个模块在 PC 端改为同一行展示。
  - 移动端继续保持自适应换行布局。

### 2026-03-29 复购与集中度图标签裁切修正

- **反馈来源**:
  - 复购与集中模块右侧的集中度条形图在 60%~70% 区间时，右侧百分比标签仍然贴边。

- **本次完成**:
  - 继续增大集中度图的右侧留白。
  - 将集中度图的 X 轴上限放宽到 `110`，给百分比标签提供缓冲区。

- **验证结果**:
  - `web/` 目录执行 `npm run lint -- src/features/consumption/components/ConsumptionDefaultTheme.tsx src/features/shared/mockData.ts` 通过。
  - `web/` 目录执行 `npm run typecheck` 通过。
  - `web/` 目录执行 `npm run build` 通过。

### 2026-03-29 前端构建恢复

- **问题定位**:
  - 用户侧看到的是 `web/package.json` BOM 报错。
  - 本地复核后确认 `web/package.json` 已可正常 JSON 解析，真正阻塞构建的是 `SavingsGoalDialog.tsx` 与 `SavingsPlanDialog.tsx` 的语法损坏。

- **本次完成**:
  - 重新写回 `web/package.json` 为 UTF-8 无 BOM 并确认可解析。
  - 修复储蓄弹窗组件中的坏字符串和非法 JSX，恢复前端构建。

- **验证结果**:
  - `web/` 目录执行 `npm run build` 通过。
