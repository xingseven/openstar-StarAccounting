# 多账户账单数据分离权限系统设计

## 一、现状分析

### 1.1 现有数据表 userId 关联情况

| 表名 | userId 字段 | 外键约束 | 数据隔离状态 |
|------|-------------|----------|--------------|
| transaction | ✓ | ✓ | 已隔离 |
| asset | ✓ | ✓ | 已隔离 |
| loan | ✓ | ✓ | 已隔离 |
| budget | ✓ | ✓ | 已隔离 |
| savingsgoal | ✓ | ✓ | 已隔离 |
| savingsplan | ✓ (间接) | ✓ (通过 goalId) | 已隔离 |
| themeconfig | ✓ | ✓ | 已隔离 |
| appconnection | ✓ | ✓ | 已隔离 |
| exchangerate | ✗ | - | 系统级表 |

**结论：** 现有表结构已满足"每个用户只能访问自己数据"的隔离需求。

---

## 二、设计目标

支持以下场景：
1. **家庭共享**：多个用户共享一个账户，各自查看/管理自己的账单数据
2. **权限分级**：账户所有者（Owner）、管理员（Admin）、成员（Member）三种角色
3. **数据独立**：成员之间账单数据互不可见，除非被标记为共享
4. **独立配置**：每个成员有独立的主题、预算、资产视图配置

---

## 三、数据库设计

### 3.1 新增表：account（账户）

```sql
CREATE TABLE `account` (
  `id` varchar(191) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '账户ID',
  `name` varchar(191) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '账户名称',
  `ownerId` varchar(191) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '所有者用户ID',
  `createdAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '创建时间',
  `updatedAt` datetime(3) NOT NULL COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `Account_ownerId_idx` (`ownerId`),
  CONSTRAINT `Account_ownerId_fkey` FOREIGN KEY (`ownerId`) REFERENCES `user` (`id`) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='账户信息';
```

### 3.2 新增表：account_member（账户成员）

```sql
CREATE TABLE `account_member` (
  `id` varchar(191) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '成员记录ID',
  `accountId` varchar(191) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '账户ID',
  `userId` varchar(191) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '用户ID',
  `role` enum('OWNER','ADMIN','MEMBER') COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'MEMBER' COMMENT '角色',
  `nickname` varchar(191) COLLATE utf8mb4_unicode_ci DEFAULT NULL COMMENT '在账户中的昵称',
  `canViewOwn` tinyint(1) NOT NULL DEFAULT '1' COMMENT '可查看自己的账单',
  `canManageOwn` tinyint(1) NOT NULL DEFAULT '1' COMMENT '可管理自己的账单',
  `canViewAll` tinyint(1) NOT NULL DEFAULT '0' COMMENT '可查看所有成员账单',
  `canManageAll` tinyint(1) NOT NULL DEFAULT '0' COMMENT '可管理所有成员账单',
  `joinedAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '加入时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `AccountMember_accountId_userId_key` (`accountId`,`userId`),
  KEY `AccountMember_userId_idx` (`userId`),
  CONSTRAINT `AccountMember_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE,
  CONSTRAINT `AccountMember_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='账户成员关系';
```

### 3.3 修改表：user（用户表）

新增字段关联默认账户：

```sql
ALTER TABLE `user` ADD COLUMN `defaultAccountId` varchar(191) DEFAULT NULL COMMENT '默认账户ID' AFTER `name`;
ALTER TABLE `user` ADD CONSTRAINT `User_defaultAccountId_fkey` FOREIGN KEY (`defaultAccountId`) REFERENCES `account` (`id`) ON DELETE SET NULL ON UPDATE CASCADE;
```

### 3.4 修改业务表：新增 accountId 字段

现有业务表的 userId 外键保留，追加 accountId 支持跨成员查询：

```sql
-- transaction 表
ALTER TABLE `transaction` ADD COLUMN `accountId` varchar(191) NOT NULL COMMENT '账户ID' AFTER `userId`;
ALTER TABLE `transaction` DROP FOREIGN KEY `Transaction_userId_fkey`;
ALTER TABLE `transaction` ADD CONSTRAINT `Transaction_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `transaction` ADD CONSTRAINT `Transaction_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;

-- asset 表
ALTER TABLE `asset` ADD COLUMN `accountId` varchar(191) NOT NULL COMMENT '账户ID' AFTER `userId`;
ALTER TABLE `asset` DROP FOREIGN KEY `Asset_userId_fkey`;
ALTER TABLE `asset` ADD CONSTRAINT `Asset_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `asset` ADD CONSTRAINT `Asset_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;

-- loan 表
ALTER TABLE `loan` ADD COLUMN `accountId` varchar(191) NOT NULL COMMENT '账户ID' AFTER `userId`;
ALTER TABLE `loan` DROP FOREIGN KEY `Loan_userId_fkey`;
ALTER TABLE `loan` ADD CONSTRAINT `Loan_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `loan` ADD CONSTRAINT `Loan_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;

-- budget 表
ALTER TABLE `budget` ADD COLUMN `accountId` varchar(191) NOT NULL COMMENT '账户ID' AFTER `userId`;
ALTER TABLE `budget` DROP FOREIGN KEY `Budget_userId_fkey`;
ALTER TABLE `budget` ADD CONSTRAINT `Budget_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `budget` ADD CONSTRAINT `Budget_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;

-- savingsgoal 表
ALTER TABLE `savingsgoal` ADD COLUMN `accountId` varchar(191) NOT NULL COMMENT '账户ID' AFTER `userId`;
ALTER TABLE `savingsgoal` DROP FOREIGN KEY `SavingsGoal_userId_fkey`;
ALTER TABLE `savingsgoal` ADD CONSTRAINT `SavingsGoal_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `SavingsGoal_userId_fkey` ADD CONSTRAINT `SavingsGoal_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;

-- themeconfig 表
ALTER TABLE `themeconfig` ADD COLUMN `accountId` varchar(191) DEFAULT NULL COMMENT '账户ID' AFTER `userId`;
ALTER TABLE `themeconfig` DROP FOREIGN KEY `ThemeConfig_userId_fkey`;
ALTER TABLE `themeconfig` ADD CONSTRAINT `ThemeConfig_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `account` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE `themeconfig` ADD CONSTRAINT `ThemeConfig_userId_fkey` FOREIGN KEY (`userId`) REFERENCES `user` (`id`) ON DELETE CASCADE ON UPDATE CASCADE;
```

---

## 四、角色权限矩阵

| 权限 | OWNER | ADMIN | MEMBER |
|------|-------|-------|--------|
| 管理账户设置 | ✓ | ✗ | ✗ |
| 邀请/移除成员 | ✓ | ✓ | ✗ |
| 设置成员权限 | ✓ | ✗ | ✗ |
| 查看所有成员账单 | ✓ | 可配置 | ✗ |
| 管理所有成员账单 | ✓ | ✗ | ✗ |
| 查看自己账单 | ✓ | ✓ | ✓ |
| 管理自己账单 | ✓ | ✓ | ✓ |
| 退出账户 | ✗ | ✓ | ✓ |

---

## 五、API 设计

### 5.1 账户管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/accounts | 创建账户 |
| GET | /api/accounts | 获取用户所有账户 |
| GET | /api/accounts/:id | 获取账户详情 |
| PUT | /api/accounts/:id | 更新账户 |
| DELETE | /api/accounts/:id | 删除账户（仅 Owner） |

### 5.2 成员管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/accounts/:id/members | 获取成员列表 |
| POST | /api/accounts/:id/members | 邀请成员 |
| PUT | /api/accounts/:id/members/:userId | 更新成员角色/权限 |
| DELETE | /api/accounts/:id/members/:userId | 移除成员 |

### 5.3 权限验证中间件

```typescript
// 伪代码：权限检查逻辑
function checkAccountPermission(userId: string, accountId: string, action: string): boolean {
  const member = getMember(userId, accountId);
  if (!member) return false;

  switch (action) {
    case 'view_own':
    case 'manage_own':
      return member.canViewOwn || member.canManageOwn;
    case 'view_all':
      return member.role === 'OWNER' || member.canViewAll;
    case 'manage_all':
      return member.role === 'OWNER';
    case 'manage_members':
      return member.role === 'OWNER' || member.role === 'ADMIN';
    default:
      return false;
  }
}
```

---

## 六、数据查询示例

### 6.1 获取用户在某账户下可见的账单

```typescript
// 获取当前用户在某账户的成员信息
const member = await getMember(userId, accountId);

let query;
if (member.role === 'OWNER' || member.canViewAll) {
  // 可查看所有成员账单
  query = { accountId };
} else {
  // 仅查看自己的账单
  query = { accountId, userId };
}

const transactions = await db.transaction.findMany({ where: query });
```

### 6.2 切换默认账户

```typescript
// 用户切换默认账户
await db.user.update({
  where: { id: userId },
  data: { defaultAccountId: newAccountId }
});
```

---

## 七、迁移步骤

### Phase 1: 数据结构变更
1. 创建 `account` 表
2. 创建 `account_member` 表
3. 为现有业务表添加 `accountId` 字段
4. 建立外键关系

### Phase 2: 数据迁移
1. 为每个现有用户创建默认账户
2. 将用户现有数据迁移到对应账户
3. 更新 `user.defaultAccountId`

### Phase 3: API 开发
1. 实现账户 CRUD API
2. 实现成员管理 API
3. 实现权限验证中间件
4. 修改现有 API 支持 accountId 过滤

### Phase 4: 前端适配
1. 添加账户切换组件
2. 添加成员管理页面
3. 修改数据查询逻辑

---

## 八、注意事项

1. **向后兼容**：现有单用户场景无需改动，数据自动迁移后原功能保持正常
2. **事务处理**：迁移过程需要事务保证数据一致性
3. **索引优化**：accountId + userId 复合索引确保查询性能
4. **级联删除**：删除账户时自动清理关联的业务数据
